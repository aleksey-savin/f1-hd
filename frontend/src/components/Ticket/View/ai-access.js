/**
 * Кому в карточке заявки доступны функции ИИ — руководство, понятия, замечания
 * и расшифровка аудио: исполнителю с правом «Пользоваться функциями ИИ».
 *
 * Запрос один на все пять мест, чтобы они не разошлись: тот же вопрос задаёт
 * сервер (`routes/internal/ticket.js`: `canPerformTickets` + `canUseAi`), а
 * без права он ещё и не отдаёт руководство с понятиями в данных заявки.
 * Включена ли сама функция — отдельный вопрос (`prefs.ai.features`).
 */
export const AI_ACCESS = { ticket: ["perform"], ai: ["use"] };
