/**
 * Линия ожидания — на нижней границе бара оболочки (макет «Индикатор
 * перехода», вариант B: линия стоит на черте, под которой появится страница,
 * и стоит там одинаково на десктопе, в браузере телефона и в PWA).
 *
 * Появляется, когда ожидание длится дольше 200 мс: признак `data-navigating`
 * на корне документа ставит `layout/Root.jsx`, счётчик ожидающих и пороги —
 * `app/nav-wait`. Геометрия — классами здесь, движение — в index.css.
 *
 * Полоса лежит во всю ширину и сжата `scaleX(0)`: заполнение — transform, а
 * не width (макет «Плавность перехода»), поэтому нулевую ширину задавать
 * нельзя — масштабировать было бы нечего. В покое линия прозрачна, кликов не
 * ловит и места не занимает. Хозяева — оба заголовка `layout/Navbar.jsx`.
 */
const NavProgress = () => (
  <div
    aria-hidden
    className="nav-progress pointer-events-none absolute inset-x-0 -bottom-px h-0.5 w-full bg-primary opacity-0"
  />
);

export default NavProgress;
