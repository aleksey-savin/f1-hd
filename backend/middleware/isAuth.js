// Личность устанавливает attachSession (один раз на весь /api), а здесь —
// только отказ при её отсутствии. Имя сохранено: его знают 52 маршрута,
// переименование в requireAuth пойдёт отдельным механическим коммитом.
module.exports = require("./requireAuth");
