# UX/UI Guide

Документ описывает UI-конвенции приложения, как они сложились в коде, чтобы
новые экраны выглядели и работали единообразно. Это свод правил, а не строгая
спецификация: при расхождении с кодом прав код — ориентируйтесь на него и
обновляйте этот файл. Хронология решений — в «[Журнале изменений](#журнал-изменений)»
в конце; статус миграции — в разделе миграции ниже.

_Последняя сверка с кодовой базой: 2026-07-22._

## Стек и базовые принципы

- **React + react-router** (data router: `loader`/`action`, `useFetcher`,
  вложенные маршруты) — не меняется миграцией.
- **Целевой UI-стек: Tailwind v4 + shadcn/ui** (см. раздел миграции ниже) —
  им строятся все новые и мигрированные экраны. **react-bootstrap** доживает
  на немигрированных экранах; его конвенции гайд не описывает — новое в
  легаси-областях делается по образцу соседнего кода (см. правила миграции).
- **react-icons**, набор **Remix (`Ri*`)** — источник иконок приложения
  (подробнее — раздел «Иконки»).
- **zustand** для UI-состояния (`store/offcanvas.js`, `store/toast-store.js` …),
  **Context** для темы (`store/theme-context.jsx`; он же ставит класс `.dark`
  на `<html>`).
- **react-select** только через обёртку `UI/Select.jsx` (стилизована
  css-переменными новой темы, меню в портале) — голый `react-select` не
  использовать. Обёртка общая для обеих систем до замены на Combobox.
- Отдельные ветки **`BrowserView` / `MobileView`** (`react-device-detect`) там,
  где десктоп и мобайл расходятся по раскладке.
- Весь UI-текст — **на русском**. Семантику передаём вариантами компонентов и
  токенами (`primary`/`success`/`destructive`…), а не произвольными цветами.

---

## Миграция на Tailwind v4 + shadcn/ui (идёт с 2026-07)

Приложение поэтапно переезжает с react-bootstrap на Tailwind v4 + shadcn/ui
(области мигрируют по одной; layout сохраняется, поверхности — в духе
согласованного макета: панели с тонкими границами на канве, без теней).

Эталоны: **списки** — справочник «Вендоры» (`pages/Vendor/*`, пилот;
серверная выборка — «Пользователи», богатая строка — «Компании»),
**настройки** — «Мой аккаунт» (`pages/User/MyAccount.jsx`), **карточка** —
«Услуга» (`ServicePlan/View.jsx`), **мастер** — `ServicePlan/Form.jsx`.

**Мигрировано на сегодня**: справочники инвентаря (вендоры, типы, модели,
атрибуты, расположения), категории заявок, шаблоны заявок, регламентные
задания, услуги, пользователи (список, карточка, формы, «Мой аккаунт»),
компании целиком (список, карточка, формы, лог активности), архив заявок,
настройки системы, мониторинг Mikrotik (список, страница записи, формы),
отчёт «Аналитика» (сводка и динамика — пилот правил страниц-отчётов).
Источник истины по путям и ширинам — `MIGRATED_ROUTES` в `layout/Root.jsx`.

**Инфраструктура:**

- Токены темы — `src/styles/tailwind.css` (`:root`/`.dark`; класс `.dark` на
  `<html>` ставит `ThemeProvider`). Радиус `0.5rem`, шрифт — **Inter**
  (self-hosted `@fontsource-variable/inter`), он же включён и для
  легаси-экранов через `html:root { --bs-font-sans-serif }`.
- Все tailwind-утилиты — с префиксом **`tw:`** (`tw:flex`, `tw:hover:bg-accent`,
  `tw:dark:border-input`) и **только из встроенной сетки** — произвольных
  значений вида `text-[15px]` не пишем. Префикс снимется автозаменой в конце
  миграции. `cn()` — из `@/lib/utils` (twMerge знает префикс).
- shadcn-компоненты — `src/components/ui/*.tsx` (генерирует CLI, префикс
  ставится сам): Button/Badge/Alert расширены вариантами `success`/`warning`/
  `info` (+ `light` у Alert). Сгенерированный код руками не перекраиваем —
  расхождения с доками shadcn чинятся токенами или точечно (см. ниже).
- **Preflight ВЫКЛЮЧЕН до эндшпиля** — из этого следуют пять ловушек, уже
  наступленные и закрытые:
  1) браузерные дефолты `<button>`/`<a>` никто не сбрасывает — интерактивные
     элементы задают `appearance/border/bg/no-underline` (и `p-0` у
     кнопок-контролов фиксированного размера: switch/checkbox/radio) явно;
  2) вендоренный bootstrap уложен в css-слой `layer(bootstrap)` — без этого
     его reboot перебивал бы tw-утилиты;
  3) типографику и отступы в tw-коде задаём явно (`tw:space-y-*`, `tw:my-0`);
  4) голый `tw:border` (так пишут сгенерированные shadcn-компоненты) без
     preflight падал в `currentColor` — светлые рамки меню в тёмной теме;
     восстановлено правилом `*,::before,::after { border-color: var(--border) }`
     в `@layer base` (`styles/tailwind.css`) — только цвет, width/style
     остаются за bootstrap reboot;
  5) `tw:border-dashed`/`tw:border-solid` — это border-STYLE-шорткаты, ставят
     стиль всем 4 сторонам. Без preflight-сброса `border-width:0` стороны без
     явной ширины рисуются дефолтной (medium ≈ 3px): `tw:border-t
     tw:border-dashed` даёт не верхнюю линию, а целый бокс. Односторонний
     пунктир задаём инлайном (`style={{ borderTop: "1px dashed var(--border)" }}`),
     не классами (эталон — `ServicePlan/View.jsx`, разделитель нерабочего
     времени).
- z-index: radix-оверлеи подняты до 1100 (блок `[data-slot=…]` в
  `tailwind.css`), выше bootstrap-модалок (1045–1055). Бар оболочки —
  инлайн `zIndex: 1030` (легаси-шкала: выше рейла статусов 1020, ниже
  модалок) — tw-сетка z заканчивается на 50.
- **Скролл-лок radix**: `ui/dropdown-menu` переведён на `modal={false}` по
  умолчанию (меню не должно лочить скролл). Модальные оверлеи
  (Dialog/AlertDialog/Sheet) лочат законно — «прыжки» лэйаута сняты
  системно: на десктопе желоб скроллбара есть всегда (`html { overflow-y:
  scroll }`), а компенсирующий `padding-right` от react-remove-scroll погашен
  (`body[data-scroll-locked] { padding-right: 0 !important }`) — при локе
  ничего не сдвигается ни на пиксель. Пиксельные компенсации фиксед-элементов
  не использовать — они не покрывают фоновую картинку и полосят.
- **Порталы внутри модальных radix-оверлеев некликабельны** (pointer-events:
  none на body + закрытие по «клику снаружи»). `UI/Select` в `FormSheet` и в
  Sheet фильтра переключается на инлайн-меню через `InsideOverlayContext`
  (`components/app/overlay-context.ts`) — новые модальные обёртки со своими
  порталами обязаны ставить этот Provider.
- **Палитра статусов присутствия — per-theme токены `--ws-st-*`**
  (`styles/tailwind.css`): каталог `util/work-statuses.js` отдаёт
  `color: "var(--ws-st-…)"` — годится только для CSS-контекстов; копии
  каталога у бота/бэкенда остаются на hex. В `index.css` от бара статусов
  (`User/WorkStatusBar.jsx`) — только вне-сеточное: `.has-ws-rail` (резерв
  92px), `.ws-rail-scroll`, `.ws-strip-scroll`, `.ws-avatar`/`.ws-live`.
- Мигрированные маршруты перечислены в `layout/Root.jsx` и рендерятся **на
  канве без bootstrap-Card**; из-за fixed-слоя фоновой картинки **любой контент
  канвы обязан быть `position: relative`** (контент-обёртка — уже в Root;
  `Footer` — сам `tw:relative` и при картинке становится плашкой-«листом»),
  иначе fixed-слой рисуется поверх и элемент «пропадает». Если у пользователя задана
  **фоновая картинка**, Root кладёт контент мигрированного маршрута на «лист»
  цвета канвы (`var(--bs-body-bg)`, скругление + тонкая граница + `p-4` —
  паттерн content sheet on wallpaper). Лист **обнимает контент, а не тянется
  на всю ширину**: `maxWidth` листа = контентная ширина страницы (её
  `tw:max-w-*`) + паддинг, значения — в конфиге `MIGRATED_ROUTES` в Root
  (новый маршрут добавляется туда парой «путь + ширина»). Обои остаются
  видимыми по бокам, пустых полей-«карточек» нет. Без картинки лист не
  рисуется — экраны про фон не знают.
- **Глобальный автоскейл `<img>`** (`index.css`: `img { width/height: auto
  !important; … }` — легаси-хак для картинок в описании тикета) перебивает
  tw-размеры на любых `<img>`. Фиксированные превью/аватары в мигрированном
  коде — `div`/`span` с `background-image` + `tw:bg-cover tw:bg-center`
  (эталон — `AccountSettings/BackgroundImageUpload.jsx`). Заскоупить хак —
  задача эндшпиля.

**Каталог компонентов `src/components/app/`** (двойники примитивов приложения;
эти — вместо ручной сборки и вместо легаси `src/UI/*`; подробные правила — в
тематических разделах, на которые ссылаются строки):

| Компонент | Назначение |
|---|---|
| `ListWrapper` | Каркас страницы-списка: заголовок + счётчик, поиск, сортировка-дропдаун, чипы (`toolbar`), кнопка создания (`addLabel`, «Новый X»), панель списка, шторка форм с `<Outlet/>` (`renderOutlet`), Sheet-фильтр, плашка `activeFilters`, пустые состояния. Серверный режим (`count`/`belowList`/`aboveList`/`hasActiveQuery`; `aboveList` — сводка выборки между плашкой фильтров и панелью, напр. «Суммарное время» работ архива) — см. «Список на серверной выборке». Контракты легаси сохранены (`filterStore`, `store/offcanvas.js`) |
| `FormWrapper` | Оболочка формы: `fetcher.Form`, заголовок, ошибки/успех, «Отмена»/«Сохранить», закрытие и навигация по успеху — см. «Формы → нижняя шторка» |
| `FormSheet` | Нижняя шторка форм: десктоп — колонка 672px (`wide` → 896px), мобайл — почти весь экран; поведение закрытия — см. «Формы → нижняя шторка» |
| `Field` | Поле формы: лейбл + `required`-звёздочка + контрол + `hint` |
| `SwitchField` | Строка свитча: свитч + подпись + hint + скрытый input `"true"/"false"` для FormData (`name`) |
| `ListRow` | Строка списка: монограмма-плитка, имя + мета, «⋯»-меню (Изменить/Удалить + `extraActions`); клик — правка или, с пропом `detailTo`, карточка (см. «Страница сущности»); права — `canManageEntity`. Свежесозданная/изменённая строка подсвечивается сама (окно 8с по `createdAt`/`updatedAt` — API списка обязан их отдавать); анимации `row-flash`/`row-appear` заданы один раз в `styles/tailwind.css` — на страницах не переопределять |
| `ListGroupLabel` | Uppercase-метка группы со счётчиком (`tone="on"/"off"`) |
| `FilterChip` | Чип-фильтр с точкой-индикатором (`active`) |
| `ChipSelect` | Дропдаун-чип **одиночного выбора без поиска** — ключевой фасет с коротким набором значений; «Все …» сбрасывает (`clearable`). См. «Ключевой фасет» в анатомии списка |
| `ChipCombobox` | Дропдаун-чип с **поиском** (Popover + shadcn Command/cmdk) — фасет с длинным списком значений; `clearable={false}` — обязательный контекст без пункта сброса |
| `ChipMultiCombobox` | **Мульти**-фасет с поиском, значение — массив id (компании/пользователи в фильтре шаблонов) |
| `DeleteItem` / `DeleteDialog` | Подтверждение удаления — см. «Диалоги» |
| `SettingsSection` / `SettingRow` | Секция страницы настроек (uppercase-метка + панель) и её строка (название + hint + контрол справа; `divider`; на узких экранах — столбец) — см. «Мой аккаунт» |
| `PhoneInput` | tw-двойник `UI/PhoneInput` (маска телефона на shadcn Input) |
| `Segmented` | Сегментный переключатель: опции `{value,label}` (+ `icon`, вертикальный `stacked`); `ThemeSegment` — готовый частный случай для темы light/dark/system (`showLabels={false}` — только иконки) |
| `MonthStepper` | Чип-степпер периода «‹ июнь 2026 ›» для строки инструментов списка: value — `{from,to}` (yyyy-MM-dd, общие с Sheet-фильтром), стрелки шагают по календарным месяцам (вперёд — не дальше текущего), произвольный период — диапазоном дат, пустой — «Весь период»; дефолт периода задаёт стор (`util/period.js`, прошлый месяц у архива) |
| `WizardStepper` | Горизонтальный степпер мастера — правила и стили в «Большие многошаговые формы» |
| `Checklist` | Чек-лист заявки/шаблона/регламента (режимы run/edit/read) — см. «Переиспользуемые доменные блоки» |
| `Environment` | Виджет «Окружение» (семантический zoom по физической иерархии) для заявки, карточек пользователя и компании (+ `EnvironmentDeviceTile`/`EnvironmentDeviceSheet`) — см. «Переиспользуемые доменные блоки» |
| `TechSection` | Секция «Техника» карточки (компания/пользователь): виды «Список» (строки + фасеты страницы «Устройства») и «Окружение», общая шторка устройства — см. «Переиспользуемые доменные блоки» |
| `CustomFieldsEditor` / `CustomFieldsView` | Кастомные поля: builder и read-показ — см. «Переиспользуемые доменные блоки» |
| `ScheduleBuilder` | Конструктор cron-расписания — см. «Переиспользуемые доменные блоки» |
| `ScheduleEditor` | Недельный график (строка на день: чекбокс · время · «24 часа») — делят мастер услуги и форма компании; формат дней = `workSchedule`/`customProvisionSchedule` |
| `Panel` / `Eyebrow` / `SubLabel` | Панель-секция карточки (`rounded-xl border bg-card`) + uppercase-метка над ней (проп `id` — якорь для `AnchorRail`; `action` — контрол секции справа, outline: залитая кнопка на экране одна — у hero) + метка подгруппы внутри панели |
| `PropRow` | Строка карточки «плитка-иконка · подпись · значение» (+ `copy` с тостом, `action` — контрол справа); общая для карточек пользователя и компании |
| `StatTile` | Статборд-плитка KPI: значение крупно + строка дельты (`StatTileDelta`: рост — accent-text, падение — destructive, знак дублирует цвет, нет базы — «—») + сноска; `busy` приглушает на фоновой подгрузке. Делят «Активность» карточки компании и KPI-ряд отчётов — см. «Страница отчёта и графики» |
| `Pager` | Постраничность списка на серверной выборке: номера страниц (десктоп) + «Показать ещё» (мобайл) — слот `belowList` у `ListWrapper`; порцию и `total` считает сервер. Используют «Пользователи» и «Архив заявок» |
| `PillPanel` | Секция-панель нейтральных пилюль с автосворачиванием («Показать все (N)») — см. «Страница сущности» |
| `AnchorRail` | Липкий рейл-якорь длинных страниц с секциями — см. «Страница сущности» |
| `AppBanner` | Баннер уровня оболочки («Доступна новая версия», сервисные оповещения): непрозрачная карточка-«лист» поверх любых обоев, тон несёт только плитка-иконка, справа действие + крестик `onDismiss`. Рендерит `layout/Root.jsx` |
| `SearchBar`, `AlertMessage`, `Spinner`, `FilterContainer` | Поиск с иконкой; алерт с маппингом bootstrap-вариантов; спиннер; тело Sheet-фильтра с «Применить/Сбросить» |
| утилиты | `monogram.ts` (`monogramFor`), `entity-permissions.ts` (`canManageEntity`), `overlay-context.ts` (`InsideOverlayContext` — см. «порталы внутри модальных оверлеев» выше) |

Если в новом экране появляется повторяемый кусок — он **сразу** выделяется в
`components/app`, а не копируется по страницам.

**Правила:**

- **Каждый экран миграции начинается с HTML-макета** (артефакт: десктоп +
  мобайл, обе темы), который согласовывается с Алексеем до написания кода;
  код повторяет макет пиксельно. Эталон языка — макет «Вендоров»: группы-метки
  со счётчиками, монограммы, цветной статус-текст вместо заливных бейджей,
  бирюзовое главное действие.
- Мигрированный файл: **ни одного импорта react-bootstrap и ни одного
  bootstrap-класса**; утилиты только `tw:*`, компоненты — shadcn/каталог выше.
- В легаси-экраны `tw:`-классы не добавлять; новые фичи в мигрированных
  областях — сразу на tw, в немигрированных — по-старому.
- Radix-оверлеи не открывать из bootstrap-модалок (и наоборот).
- `UI/Select` (react-select) остаётся общим для обеих систем до эндшпиля.
- **Верификация**: `pnpm build` + `pnpm eslint` — и передать Алексею на
  проверку в живом UI (он проверяет сам и говорит, если что-то не так).
  Playwright-проходку самостоятельно не устраивать — только по явной просьбе
  (рецепт: mint JWT в `hd-backend-1`, `addInitScript` с localStorage).

---

## Когда что использовать (главное правило выбора контейнера)

| Ситуация | Контейнер | Референс |
|---|---|---|
| Подтверждение, короткий вопрос, мелкий диалог (1–3 поля) | **AlertDialog/Dialog** | `components/app/DeleteItem.tsx` |
| Заполнение/редактирование формы (add/update сущности) | **Нижняя шторка** `app/FormSheet` (в `app/ListWrapper` встроена) | `pages/Vendor/*` |
| Большая многошаговая форма (мастер) | **Расширенная** шторка + степпер во всю ширину + живая сводка справа | `ServicePlan/Form.jsx` |
| Фильтры списка (общие для десктопа и мобайла) | Sheet сбоку (в `app/ListWrapper`) | `components/app/ListWrapper.tsx` |
| Предпросмотр записи списка/дерева («заглянуть», не уходя) | **Шторка справа**: крошки/статус/ключевые поля + вложенная навигация; главное действие — «Открыть карточку» | `components/Location/PreviewSheet.jsx` |
| Список/таблица как «всплывающая» панель | Шторка снизу | `layout/Dashboard/Offcanvas.jsx` (легаси-область) |
| Правка **документа** (заметка, статья) | **На месте, на той же странице** | `components/KnowledgeBase/NoteView.jsx` (легаси-область) |
| Массовые действия над выделением | Плавающая панель (десктоп) + `UI/MobileActionBar` | `components/Ticket/BulkActionBar.jsx` (легаси-область) |

Коротко, три опорных правила:

1. **Диалог — только для маленьких вещей**: подтверждения, единичный вопрос,
   быстрое действие. Не размещать в диалоге полноценные формы.
2. **Заполнение форм — в шторке, выезжающей снизу**; на десктопе клик мимо её
   не закрывает, `Escape` — закрывает (детали — «Формы → нижняя шторка»).
3. **Большая многошаговая форма — со степпером во всю ширину шторки и живой
   сводкой справа** (см. «Большие многошаговые формы»).

---

## Раскладка страницы

`layout/Root.jsx` задаёт каркас:

- **Десктоп** (`BrowserView`): фиксированный **бар оболочки на tw**
  (`layout/Navbar.jsx`): панель поверхности 56px с тонкой нижней
  границей в обеих темах; бренд — лого компании (`contacts.logo` из настроек,
  локальный файл) или текст «Help**Desk**»; пункты и разделы — из конфига
  `layout/Navigation/menu.js` (иконки только `Ri*`; раздел виден, если виден
  хотя бы один его пункт; группа раздела — `{ label?, items }` с
  uppercase-заголовком `label`; бар показывает `shortLabel ?? label`, полные
  названия — в дропдаунах и drawer); на **< xl** меню сворачивается в бургер-Sheet
  (`layout/NavDrawer.jsx`, общий с мобильным шеллом). Меню пользователя —
  **Popover, не DropdownMenu** (внутри инпут заметки статуса, radix-меню
  ломает его typeahead'ом). Контент — `paddingTop: 100px`; слева опционально
  колонка-сайдбар (`leftSidebarContent`). `Container fluid`,
  `maxWidth: 1920px`, отступы `px-5`. Переходы — `animations/Transition`.
  **Мигрированные маршруты** (список в Root) рендерятся прямо **на канве без
  bootstrap-Card** — панель рисует сам экран (`app/ListWrapper`); легаси-экраны
  по-прежнему живут в `Card.shadow`.
- **Мобайл** (`MobileView`): нескроллящийся app-shell — см. раздел
  «Мобильная навигация».
- Тосты — sonner, снизу справа (см. «Обратная связь»).

Списочные страницы собираются `components/app/ListWrapper`: заголовок +
счётчик рядом, справа поиск / сортировка-текст-дропдаун / чипы / кнопка
создания «Новый …» с текстом; на мобайле кнопка создания — квадратная иконка,
поиск всегда виден.

---

## Мобильная навигация (app-shell)

На мобайле (`MobileView` в `layout/Root.jsx`) весь UI — **нескроллящийся app-shell**
`.mobile-shell` (`position:fixed; height:100dvh; display:flex; flex-direction:column;
overflow:hidden`). Скроллится только средняя область `.mobile-shell__scroll` (`<main>`);
верхний бар и нижний таб-бар — её соседи по флексу, **а не `position:fixed`**. Это
структурно убирает баг iOS Safari, где фиксированные панели «всплывали» посреди экрана
при инерционном скролле. Десктоп (`BrowserView`) сохраняет прежнюю схему (`fixed-top`
`Navbar` + `paddingTop:100px`).

```text
.mobile-shell                         position:fixed, 100dvh, flex-column, overflow:hidden
├─ <NavigationBar embedded />         верхний бар: .navbar.mobile-shell__header (в потоке)
├─ <main .mobile-shell__scroll>       единственный скролл-контейнер
│    └─ <Container> <Outlet/> <Footer/> </Container>
└─ <MobileBottomNavbar />             плавающий «остров»: .mobile-tabbar (absolute)
```

**Нижний таб-бар — `layout/MobileBottomNavbar.jsx`** («плавающий остров»):

- Скруглённый бар с `backdrop-filter: blur`, парит над краем
  (`position:absolute; bottom: calc(10px + env(safe-area-inset-bottom))`).
- Вкладка = иконка (`Ri*`/`Tb*` — см. «Иконки») + короткая подпись;
  тач-таргет ≥ 44px.
- Активная вкладка — бирюзовый акцент (`--tab-active: var(--primary)`) и
  пилюля, которая **плавно
  скользит** между вкладками через `framer-motion` `layoutId` + `LayoutGroup`
  (с уважением `useReducedMotion`). Пилюлю центрируем через `inset`, а **не**
  `transform`: framer сам управляет `transform`, иначе выделение «съезжает».
- Набор вкладок — конфиг-массив, фильтруется по правам/`isEndUser` (как в меню).
  Активность считается явно; маршруты взаимоисключающие → ровно одна активная
  вкладка (инвариант: одна пилюля на `layoutId`).
- Обязательны `aria-label` и `aria-current` на вкладках (иконки — `aria-hidden`).

**Верхний бар (мобайл)** — тот же `NavigationBar` с пропом `embedded`: статичный
флекс-ребёнок (`.mobile-shell__header`, safe-area-паддинги в `index.css`) на
tw-классах. Гамбургер + бренд + чип статуса (Popover с `WorkStatusSwitcher`).

**Бургер-меню** — `layout/NavDrawer.jsx` (Sheet слева, общий для мобильного
шелла и узкого десктопа): шапка с аватаром и ролью, скруглённые строки
навигации из конфига `menu.js` (разделы — `Collapsible`), футер —
`app/ThemeSegment` + «Мой аккаунт» + «Выйти».

**Правила (важно):**

- **Не вешать слушатели скролла на `window`** на мобайле — он не скроллится (гейт
  `isBrowser` в `UI/BackToTop.jsx`); нужен скролл — слушать `.mobile-shell__scroll`.
- Панели навигации на мобайле — **никогда не `position:fixed`** (только флекс/absolute
  внутри shell), иначе вернётся «дрейф».
- Сброс скролла при навигации — вручную (`mobileScrollRef` + `useEffect` по
  `location.pathname` в `Root.jsx`; router `ScrollRestoration` не используется).
- Тема навигации — scoped-токены `--nav-*` / `--tab-*` / `--tabbar-*` в
  `index.css` **ссылаются на токены новой системы**
  (`var(--border)`/`var(--card)`/`var(--primary)`) — тёмные значения даёт класс
  `.dark`, отдельных переопределений в `dark-theme.css` больше нет.
- `index.html`: `viewport-fit=cover` + `theme-color`; safe-area учитывается и сверху,
  и снизу.

---

## Контекстный остров действий на мобилке

`UI/MobileActionBar.jsx` — общий компонент (заявки: bulk-действия; база знаний:
bulk-модерация и правка заметки). Портал в `.mobile-shell`, классы
`.mobile-actionbar*` в `index.css`.

- **На экране всегда ровно один плавающий объект.** Пока панель в DOM,
  `.mobile-shell:has(.mobile-actionbar) .mobile-tabbar { visibility: hidden }` —
  таб-бар уступает ей место, «бутерброд» из двух плашек невозможен структурно.
- **Заблокированное действие остаётся нажимаемым и объясняет причину**: на
  тач-экране hover-тултипа нет, поэтому тап на 4 секунды подменяет строку статуса
  причиной (`reason`). Причина называет объекты **по имени** (заголовок заметки,
  номер заявки), а не по внутреннему id.
- Один массив `actions[{ key, icon, label, reason, danger }]` — источник правды и
  для десктопной плавающей панели, и для мобильного острова.
- Цветом выделено ровно одно действие (обычно разрушающее, `is-danger`); смысл
  несёт подпись под иконкой. Тач-таргет ≥ 44px, `role="toolbar"`,
  `useReducedMotion`.

---

## Анатомия списка с фильтром

Порядок сверху вниз (каркас — `app/ListWrapper`; эталоны — списки
«Пользователи» и «Компании»):

1. Кнопка создания («Новый X») и **поиск** — в шапке `ListWrapper`.
2. **Сегментный переключатель набора данных** (`app/Segmented`) там, где
   наборы взаимоисключающие («Активные | Архив»; «Все | Мои» у компаний —
   скрывается, если не сужает). Два независимых свитча для взаимоисключающих
   наборов — ошибка: их приходится синхронизировать руками, и пользователь не
   видит, что они спорят.
3. **Чипы-фасеты со счётчиками** (`app/FilterChip`; ключевой фасет —
   `app/ChipSelect`), счётчик — по загруженному набору.
4. **Очереди/спецфильтры** — только тем, у кого есть права.
5. **Sheet-фильтр с полным набором условий** — прячет длинные мультиселекты и
   отдаёт вертикаль списку (правило ниже).
6. **Сортировка** — текст-дропдаун (встроена в `ListWrapper`). Счётчик — у
   заголовка страницы.
7. Список; при группировке — заголовки групп со счётчиком
   (`app/ListGroupLabel`; статусные группы — язык статус-борда, как в
   «Вендорах»: активные бирюзой, отключённые гаснут).

Правила:

- **Не рендерить заголовок секции, если её содержимое скрыто правами.** Пустой
  `<h5>Модерация</h5>` между двумя `<hr>` у обычного сотрудника — это баг.
- **Список по умолчанию показывает всё, что пользователю видно.** Не требовать
  запроса или фильтра, чтобы данные вообще появились: бэкенд уже отдал ровно
  разрешённое, а фильтр — инструмент сужения, а не открывания.
- **Группировка выключается сама, если группа осталась одна** — заголовок с
  единственной группой не несёт информации.
- **Пустое состояние предлагает действие**: «Ничего не нашлось. Измените запрос
  или сбросьте фильтры», а не «Список пуст». В `ListWrapper` оба встроены:
  «Ничего не нашлось» + «Сбросить фильтры» при отфильтрованном в ноль
  (`hasActiveQuery`), «Список пуст» + кнопка создания при пустых данных.
- Мобильная страница списка собирается тем же `ListWrapper` (в мигрированном —
  поиск всегда виден, кнопка «Фильтр» → Sheet с `FilterContainer`, сортировка
  справа). Если страница рендерит `<Outlet/>` сама — передайте
  `renderOutlet={false}`, иначе маршрут смонтируется дважды.
- **Фильтр есть на каждой странице справочника**: Sheet (`filter` проп
  `ListWrapper`) с полным набором, общий для десктопа и мобайла. Дублирующие
  чипы-переключатели в строке инструментов не ставим — применённое видно в
  плашке бейджей (`app/FilterChip` остаётся для мест вне `ListWrapper`).
- **Ключевой фасет сущности** (взаимоисключающие значения) выводим одним из двух
  способов — выбор фиксируем при согласовании макета, единообразно в пределах
  страницы:
  - **дропдаун-чип `app/ChipSelect` в строке инструментов** (`toolbar` проп) —
    быстрый доступ рядом с поиском; выбранное видно в самом чипе, сброс —
    пунктом «Все …». Живой пример: «Тип устройства» на странице атрибутов
    (`pages/DeviceAttribute/List.jsx`);
  - **одиночный `UI/Select` внутри Sheet-фильтра** — когда фасет уместнее рядом
    с прочими условиями, а не отдельным чипом; первая опция «Любое/Все …»
    сбрасывает фасет. Живой пример: «Назначение» на странице типов
    (`components/DeviceType/Filter.jsx`; «Основные устройства» = тип без
    спец-флагов). Список значений фасета выносим в отдельный модуль
    (`components/DeviceType/kinds.js`), раз его читают и фильтр, и бейдж.
  В обоих случаях применённое значение дублируется **снимаемым бейджем** в
  липкой плашке (`activeFilters`), а «Дублирующих чипов-переключателей» правило
  выше не запрещает — оно про повторную индикацию уже применённого, а не про
  сам ключевой фасет.
- **Применённые фильтры всегда видны**: страница передаёт в `ListWrapper`
  `activeFilters=[{ key, label, onRemove }]` — липкий «остров» бейджей над
  списком (виден и при проскроленном в конец списке), каждый бейдж снимается
  крестиком, рядом «Сбросить» (сбрасывает стор и очищает поиск). Лейблы —
  человеческие («Тип: Число»), не значения полей.

### Бюджет управляющих элементов

Считайте пиксели до первой строки данных. Каждый контрол в этом списке стоит
40–50 px, и все они кажутся полезными по отдельности: кнопка создания, поиск,
переключатель набора, чипы фасетов, очереди, аккордеон фильтров, сортировка.
Семь рядов — и в колонке шириной в треть экрана видно три записи из двухсот.

- **Главное действие — иконка рядом с поиском**, а не залитая кнопка во всю
  ширину. Ряд экономится целиком.
- **Редко трогаемое — в Sheet-фильтр**, а плашка `activeFilters` показывает
  всё применённое, включая набор данных и выключенные фасеты. Применённый
  фильтр не бывает невидимым.
- **Набор одинаковых кнопок-режимов — в одну кнопку-меню со счётчиком.** Четыре
  очереди модерации с заголовком секции — это ~100 px; иконка со значком `5` и
  дропдаун — 31 px, и разбирают очередь не каждый день.
- **Сортировка — ссылка-дропдаун**, а не полноразмерный селект на 38 px: её
  меняют раз в сессию.
- **Строка состояния вместо всегда видимых индикаторов**: контекст («Архив»,
  «Очередь: На проверку»), «Найдено: N», «Сбросить» — только когда есть что
  сбрасывать. В чистом состоянии эта строка почти пуста и ничего не стоит.

Правило выведено на редизайне базы знаний (`Explorer.jsx`): было ~370 px до
первой заметки, стало ~110 px.

### Когда список перестаёт помещаться на экран

Сотня строк — ещё список. Несколько сотен — уже дерево, и вертикаль кончается
раньше данных. Три правила, в этом порядке:

1. **Группы свёрнуты по умолчанию — все.** Список свёрнутых групп помещается на
   экран целиком и сам работает навигацией. Раскрываются автоматически только
   две вещи: группа открытой сейчас сущности (иначе deep-link ведёт в невидимую
   строку) и — пока идёт поиск — группы с совпадениями.
2. **Раскрытие поиском — настоящее, а не подменённый флаг.** Заголовок группы,
   который выглядит кликабельным и не реагирует на клик, — баг. Раскрывайте
   группы вызовом, а при очистке запроса сворачивайте обратно: их раскрыл поиск,
   а не человек.
3. **На узком экране дерево превращается в drill-down.** Раскрытая группа на
   мобилке не влезает, а строка «Студия · 21» влезает вся: сначала список групп
   со счётчиками, тапом — содержимое группы, тапом — сущность. Поиск и очереди
   на модерацию из drill-down выпрыгивают в плоский список: там ось группировки
   не отвечает на заданный вопрос.

Живой пример — секция «Модели устройств» на карточке вендора
(`Vendor/View.jsx`): группы-аккордеоны по типам устройств, малый каталог
(≤ 12 моделей) раскрыт сразу, крупная группа (> 8) отдаёт «Показать все N»
инлайн, поиск раскрывает группы с совпадениями, «Развернуть/Свернуть все» — в
тулбаре.

Виртуализация на этом масштабе не нужна и не решает проблему: тормозит не
браузер, а глаз. Начинайте с того, что уменьшает число строк, а не с того, что
ускоряет их отрисовку.

### Поиск переезжает на сервер вместе с данными, по которым ищет

Клиентский поиск бесплатен, только пока искать не по чему. Как только в ответе
списка появляется полный текст сущности (`plainText`, `content`, описание) —
каждый заход в раздел скачивает его целиком, ради `String.includes()` у тех
немногих, кто вообще что-то ищет.

Признак, что пора: **в списке приезжает поле, которое список не показывает.**
Тогда — `?search=` на бэкенде, поле выкидывается проекцией, а на клиенте:

- инпут обновляется мгновенно, запрос — с задержкой (300 мс);
- терм экранируется (`escapeRegex`) и ограничивается по количеству, иначе `.*`
  в поисковой строке становится сканом коллекции;
- **выбранные значения фасетов хранятся объектами, а не id.** Серверный поиск
  сужает набор, из которого собираются опции селекта; по одному id восстановить
  пилюлю выбранного значения будет уже нечем, и выбранная компания исчезнет из
  собственного фильтра, стоит начать печатать.

### Список на серверной выборке: страницы, а не «загрузить всё»

Когда список перерастает клиент (сотни записей, долгая загрузка), на сервер
уезжает **весь набор операций разом**: поиск, фасеты, сортировка и
постраничность. Половинчатый вариант не работает — клиентский поиск по
подгруженной странице ищет только по ней. Эталон — «Пользователи»
(`pages/User/List.jsx` + `controllers/user.js` `getAll`).

- **Стор держит порцию, а не весь список**: `items` (текущая страница), `total`
  (сколько нашлось всего), `page`, `pageSize`; каждое изменение фильтра,
  сортировки или страницы само делает запрос. Счётчик у заголовка — `total` с
  сервера (проп `count` у `ListWrapper`), а не длина массива.
- **Пагинация**: нумерованные страницы на десктопе, «Показать ещё» (докрутка) на
  мобайле — оба живут в `belowList` (`app/Pager`). Порция — 30 у
  «Пользователей», 50 у «Архива заявок».
- **Скоуп по правам считает БД, а не JS.** Выбрать всё и отфильтровать в памяти
  — та же «загрузка всего»: условие видимости уходит в `$match`.
- **Поиск** — `$or`-regex по полям строки; правила из раздела выше (задержка
  300 мс, экранирование и лимит терма).
- **Фоновые обновления не рвут пагинацию.** Живые статусы присутствия
  подтягиваются лёгким запросом-табло и **мержатся** в показанные строки по
  `_id`, а не перезапросом списка — иначе докрученные страницы схлопнутся.
- **Группы — только когда они не рвутся между страницами.** По умолчанию плоский
  список; группировка (например, по подразделению) включается, когда выборка
  сужена до одной компании, и тогда набор отдаётся целиком, без страниц.
- Ось, по которой нельзя отсортировать в БД, — повод **денормализовать поле**, а
  не тащить агрегат на каждый запрос (`lastActivityAt` на пользователе вместо
  join с заявками; поддерживается хуком модели при создании заявки).

### Строка-запись: жёсткие колонки

Строка справочника с несколькими колонками данных держит форму, а не плывёт по
контенту (эталон — `components/Company/Item.jsx`):

- **Все строки одной высоты.** Поле переменной длины (адрес) — в одну строку с
  усечением, полное значение — в `title`, клик открывает карту (`linkToMap`) в
  новой вкладке; усекается только **вторичное** (юрлицо), а счётчики
  («N пользователей · M услуг») закреплены в конце и не уезжают. Счётчики
  считает бэкенд (компактная проекция `usersCount`/`servicePlansCount`), а не
  длины вложенных массивов, которые списку не нужны.
- **Живой статус — «тихим форматом»**: цвет несут только точка и ключевое слово
  («открыто»), длительность приглушена («открыто · ещё 4 ч 43 мин»); негативное
  и отсутствующее («откроется через …», «график не указан») — целиком
  приглушённо, не красным. Короткую фразу отдаёт поле `detail` из
  `util/get-working-status.js` (tw-строка `Company/WorkStatusText` — список и
  шторка-справка); `verbose` остался легаси-обёртке `WorkingStatusIndicator`.
- **Гнездо действий постоянной ширины** справа (ровный правый край у всех
  строк), сами действия проявляются по наведению.
- **Действие за глобальной настройкой** (такси у компаний:
  `Preferences.taxi.operator`; глобальные префы доступны фронту через
  `preferences-initial` → `store/prefs.js`): настройка не задана — действия нет
  нигде; подписи честные при деградации данных («Такси до офиса» при известных
  координатах vs просто «Заказать такси» без них). Каталог операторов —
  `util/taxi-operators.js` (enum в `backend/models/preferences.js` синхронно).
- **Кнопки звонка в строке нет** — телефоны живут в мобильной шторке-справке
  (`Company/ContactSheet`) и на карточке.

### Строка-человек: адресная книга, а не реестр

Список людей отвечает на «кто это и как с ним связаться», поэтому отличается от
строки справочника (эталон — `components/User/Item.jsx`):

- **Круглый аватар вместо квадратной плитки-монограммы** — люди ≠ записи, лицо
  узнаётся быстрее текста. Фото — фоном на `<span>`, не `<img>`.
- **Присутствие — кольцом на аватаре и фразой** с цветной точкой (цвета — токены
  `--ws-st-*`). Расчёт «показывать ли присутствие» лежит в одном месте
  (`User/presence.js`), а не копируется по строке / шторке / карточке.
- **Контекстный правый столбец**: у сотрудника — присутствие, у клиента —
  последняя активность. Одна колонка, разный смысл по типу записи.
- **Связаться прямо из строки** (десктоп): звонок и письмо — только для реально
  заполненных каналов. Управляющие действия остаются в «⋯» под правами.
- **На мобайле тап открывает контакт-шторку** — крупные «Позвонить/Написать»,
  копирование и «Открыть профиль», не уходя со списка; сама строка компактная,
  с шевроном.

---

## Страница сущности (карточка): hero → секции → действия

Детальные страницы (услуга, пользователь, расположение, шаблон, регламент)
строятся по одному шаблону. Эталон — **`components/ServicePlan/View.jsx`**
(первая мигрированная карточка, макет согласован до кода).

- **Возврат к списку — хлебные крошки сверху** («‹ Услуги», Link на список), а
  НЕ кнопка «К списку» в действиях (дублировала бы). Крошки — по умолчанию для
  всех view-экранов.
- **Hero**: иконка/монограмма-плитка (`size-14 rounded-2xl bg-accent
  inset-ring`, та же, что в строке списка) · `h1` · «статус» строкой (цветной
  текст с точкой) · охват счётчиками (`tabular-nums`) · справа **одна залитая
  «Изменить»** + меню «⋯» (пока только Удалить, через `app/DeleteDialog`,
  рендерится вне radix-меню). Подписи «Действия» нет.
- **Секции** — панели на канве (`rounded-xl border border-border bg-card p-5`)
  с uppercase-eyebrow-меткой (+ счётчик): `Panel`/`Eyebrow` — из каталога
  `app/Panel` (их делят несколько карточек), локальный `Pill` — по месту.
  Однородные списки (категории, компании) — **нейтральными пилюлями** (не
  раскрашиваем); длинный однородный список — только через `app/PillPanel`: он
  сам сворачивает облако до ~3 рядов (`collapsedMaxHeight`), даёт
  градиент-затухание и «Показать все (N)»/«Свернуть» — вручную это не собираем.
- **Правка — в нижней шторке** прямо на карточке: `FormSheet` + `<Outlet/>` +
  `store/offcanvas` (как в `ListWrapper`); маршрут `update` вложен в `:id`.
- **Скролл к началу при монтировании** (`useEffect(() => window.scrollTo(0,0),
  [])`): на десктопе Root сбрасывает только мобильный контейнер, а window-скролл
  при навигации сохраняется — без сброса hero карточки, открытой из
  проскроленного списка, уезжает под фиксированный бар. Глобально сбрасывать по
  `pathname` нельзя — сломает фон под шторками-формами (их путь тоже меняется).
- **Строка списка ведёт на карточку** (не в шторку правки): `app/ListRow` проп
  **`detailTo`** (клик → `navigate(detailTo)`, доступно всем, кто видит список;
  «⋯» с правкой/удалением остаётся под `canManage`). В Root `MIGRATED_ROUTES`
  под общим префиксом заводим ДВЕ записи, если у списка и карточки разная
  ширина: `/…/` (со слэшем, карточка `max-w-4xl` → 944) матчится в `.find`
  раньше `/…` (список `max-w-7xl` → 1328).
- **Дочерний блок правится с карточки на месте — отдельным запросом.** Если у
  сущности есть самостоятельная под-структура (чек-лист шаблона), её правят
  прямо в секции карточки (кнопка «Изменить» → `app/Checklist` mode="edit" +
  «Сохранить …» / «Отмена»), **не открывая всю форму**. Сохранение идёт своим
  endpoint'ом (`POST …/:id/checklist`) через `useFetcher` и меняет только этот
  блок; по успеху fetcher ревалидирует loader — карточка не перезагружается,
  секция сама возвращается в чтение (`useEffect` по `fetcher.state`+`data`).
  Заголовок секции даёт карточка (`showHeader={false}` у компонента), кнопки
  правки блокируются на время сабмита. Черновик — локальный стейт, из
  `template.…` при входе в правку.
- **Длинной карточке — рейл-якорь.** Если секций много и они не влезают в экран,
  слева ставим липкий `app/AnchorRail` (десктоп; на мобайле секции просто идут
  подряд): ведёт по секциям и подсвечивает текущую (scrollspy на `window` —
  только `BrowserView`). Пункты собираются **только из реально отрисованных**
  секций — скрытая правами секция не должна оставлять пункт в рейле. Якорь
  вешается пропом `id` на `Eyebrow`/`PillPanel`/`SettingsSection` (у них же
  `scroll-mt` под fixed-навбар), а ширина карточки расширяется под рейл
  (`max-w-5xl` + соответствующая запись в `MIGRATED_ROUTES`). Табы для этого
  не заводим — см. следующее правило.
- **Вкладки — только когда у сущности есть независимые «режимы работы»**
  (карточка / мониторинг / конфигурации). Документ (заметка, статья) вкладок не
  требует: одна колонка сверху вниз читается лучше.
- **Пустое значение поля — общий `—`** (приглушённым цветом), а не пустая
  строка.
- **Пустое состояние внутри секции — не пустота**: крупная приглушённая иконка,
  заголовок, объясняющий абзац (`max-width: 480`) и главное действие. Если прав
  не хватает — вместо кнопки строка «Недостаточно прав — обратитесь к
  администратору».

---

## Действия на странице сущности

**Ровно одна залитая кнопка**, и её выбирает состояние, а не порядок в разметке.
Пример из базы знаний (`components/KnowledgeBase/NoteActions.jsx`): модератору
непроверенной заметки главное действие — «Проверить», всем остальным —
«Редактировать». Вторая кнопка — outline/ghost. Всё редкое и опасное — в
icon-only меню `⋯` (`RiMoreLine`; группы пунктов с заголовками, разрушающее —
последним и в `destructive`).

- **Подпись «Действия» запрещена.** Она ничего не сообщает: пользователь обязан
  открыть меню, чтобы узнать, что там. Icon-only «⋯» честнее и занимает меньше.
- **Чужой запрос, ожидающий решения, — инлайн-`Alert`, а не пункт меню.**
  «Пётр Петров запросил удаление 3 июля» + `Подтвердить` / `Отклонить` рядом с
  контекстом (`components/KnowledgeBase/PendingRequestAlert.jsx`). Решение,
  спрятанное в меню, принимается вслепую: не видно ни кто попросил, ни когда.
  Кнопки внутри цветного алерта наследуют его цвет (контраст по
  `currentColor`).
- Кнопка действия и её результат называются **одним словом**: «Проверить» →
  «Заметка отмечена как проверенная» → бейдж «Проверено».
- **`overflow: hidden` на блоке, где живёт кнопка меню, обрежет меню.** Обычно
  его ставят, чтобы фон-градиент не вылез за скругление, — но фон и так
  клипается по `border-radius`. Если `overflow` действительно нужен, дропдаун
  должен рендериться в портал.

---

## Статус, который протухает, — предложение, а не бейдж

Если у состояния есть **автор, дата или срок**, бейджа мало. Пишем фразу:

```text
✓ Проверено · Иванов И. · 16.06.2026 · действует ещё 24 дн.
⚠ Не проверено · изменил Петров П. · 03.07.2026
```

Эталон — `components/KnowledgeBase/VerificationLine.jsx`. Цветом красим только
иконку и само состояние; автор и дата — приглушённым (`muted-foreground`).
Срок на исходе переключает тон на `warning`, даже если состояние «хорошее».
Бейдж (`Badge`) остаётся для компактных списков, где нет места на фразу.

Чтобы фраза была возможна, бэкенд обязан отдавать **имена**, а не `ObjectId`:
`populate` актора в `getOne` и в ответах мутаций (см.
`backend/controllers/knowledgeNote.js`, `ACTOR_PATHS`). В списках имена не нужны
— там достаточно иконки, и populate туда не тащим.

---

## Правка на месте (Obsidian-режим)

Когда страница — **документ**, а не форма, правим его на месте, а не в Offcanvas.
Эталон — `components/KnowledgeBase/NoteView.jsx`.

**Главное правило: между чтением и правкой не должно двигаться ничего**, кроме
того, чем управляют. Как это достигается:

- Заголовок в правке — тот же `h2`, только инпут без рамки, с теми же
  метриками.
- Строки свойств держат `min-height` под высоту контрола выбора: пилюли в
  чтении и селект в правке занимают один бокс.
- Тело — один и тот же класс контента у вьюера и редактора
  (`.toastui-editor-contents`), поэтому мера чтения задаётся один раз.

**Клавиши и триггеры:** `Ctrl/Cmd+E` — переключить режим, `Ctrl/Cmd+S` —
сохранить, `Esc` — выйти. Двойной клик по тексту открывает правку в том же месте
(гейт по правам, не в архиве; клики по ссылкам и чекбоксам task-list
игнорируются). Точную каретку в WYSIWYG не обещаем: `EditorPos` там — offset
ProseMirror-документа, поэтому целимся в начало блока через DOM `Range`.

**Несохранённые изменения:** `useBlocker` (роутер данных) + `beforeunload` +
диалог подтверждения. Собственные переходы (сохранили новую сущность, отменили
создание) обходят блокировку через ref-флаг.

**Автосохранения нет, если у сохранения есть побочный эффект.** Правка заметки
сбрасывает отметку «Проверено» — о таком побочном эффекте предупреждаем **до**
сохранения, прямо в строке статуса: «Сохранение снимет отметку „Проверено"».

---

## Переиспользуемые доменные блоки (одна информация — один вид)

Если сущность-информация встречается у нескольких разделов (заявка, шаблон
заявки, регламентное задание), она **показывается и правится одним
компонентом**, а не переверстывается под каждый экран: пользователь не должен
потреблять одно и то же в разном виде, а мы — держать три расходящиеся со
временем копии верстки. Компонент делаем контролируемым и достаточно гибким под
все места сразу (форма данных — надмножество), а встраивание регулируем пропами
(`framed` / `showHeader` / `showProgress`), а не форком под экран.

Готовые доменные блоки (каталог `components/app`):

- **`Checklist`** — чек-лист во всех трёх ролях одним компонентом: **run** —
  выполнение в заявке (отметки, прогресс, подпись «кто · когда», гард
  обязательных), **edit** — составление в форме шаблона/регламента и правка
  чек-листа заявки (добавление по Enter, drag/Alt+↑↓, флаг «обязательный»,
  удаление с возвратом), **read** — просмотр в карточке. Контролируемый
  (`onToggle` / `onChange`); форма пункта — надмножество (`description` +
  `mandatory` + отметка `checked`/`checkedBy`/`checkedAt`); показ прогресса и
  заголовка отключаем пропами там, где секцию подписывают снаружи.
- **`Environment`** (+ `EnvironmentDeviceTile` / `EnvironmentDeviceSheet`) —
  физическое окружение (семантический zoom здание → этаж → помещение → рабочее
  место) одним виджетом на три места: вкладка «Окружение» заявки (режимы по
  заявителю `userId` и по устройству `deviceId` — приоритетный), карточка
  пользователя (`userId` + `subject="user"` — меняет подписи «заявитель/
  пользователь»), карточка компании (`companyId` — вход с обзора корневых
  расположений; endpoint `GET /locations/company/:companyId/environment`
  отдаёт корни со счётчиками по поддереву, дальше — обычный `…/:id/node`).
  Язык: линейка глубины (активный уровень — бирюзовая точка с гало),
  сцена-«план» с точечной сеткой и картографическим кластером «+/−» (колесо и
  ↑/↓ работают, `useSemanticZoom`), статусы техники — текст с точкой
  (`STATUS_META`/`mikrotikStatus`), личное — янтарная ★, целевое устройство —
  кольцо + `tw:env-aim` (пульс дважды, в `styles/tailwind.css`). Карточка
  устройства — правая radix-шторка по образцу превью расположений («Открыть
  карточку устройства» / «Открыть в мониторинге Mikrotik»). Метку секции даёт
  страница; вложенных больше 12 — список сворачивается («Показать все (N)»).
- **`TechSection`** — секция «Техника» карточки (компания `companyId` /
  пользователь `userId` + `subject="user"`): одни данные в двух представлениях,
  сегмент «Список | Окружение» в `action` метки (выбор помнится в
  localStorage). «Список» — строки с жёсткими колонками: плитка-иконка с
  **live-точкой связи Mikrotik** (язык присутствия адресной книги), имя
  (+★ личного) · «Тип · Вендор · Инв №» · расположение · учётный статус
  текстом с точкой · «↗» по наведению; клик по строке — общая
  `EnvironmentDeviceSheet`. Фасеты страницы «Устройства» минус контекст
  карточки — `ChipMultiCombobox`-чипы «Тип · Вендор · Статус · Расположение»
  (последний только у компании) + живой поиск (появляется от 9 устройств) +
  снимаемые бейджи применённого и «Сбросить»; без запроса список ограничен
  10 строками, дальше — «Вся техника в „Устройствах" (N) →» с префильтром
  `?company=` / `?user=` (страница «Устройства» читает query в стор фильтра;
  в легаси-фильтре добавлена секция «Закреплено за»). Техника пользователя —
  тремя источниками с группами от бэкенда: «Личная и рабочее место» и
  «В помещении — X» (прямая техника родителя РМ). Endpoints:
  `GET /locations/company/:companyId/tech` (порядок — DFS-обход иерархии
  расположений) и `GET /locations/user/:userId/tech`; DTO строки =
  `toEnvDevice`, шторка работает без дозапроса.
- **`CustomFieldsEditor` / `CustomFieldsView`** — кастомные поля формы: один
  builder (тип text/select/multiselect, опции, drag, «Новое поле») и один
  read-показ (имя + тип-чип + пилюли опций) на шаблон и заявку.
- **`ScheduleBuilder`** — расписание (cron-строка) одним конструктором,
  контролируемым (`value` — cron-строка, `onChange`): режим «Простой»
  (периодичность + детали) и «Строка cron» (ручной ввод); перевод на русский,
  валидация и ближайшие запуски — `util/cron.js` (пятипольный cron, TZ
  организации — как исполняет node-cron на сервере). Сейчас им собирает
  расписание форма регламента; любое новое «по расписанию» (рассылки,
  автоотчёты) обязано брать его же, а не заводить свой ввод cron.

Сопутствующие конвенции:

- **Заготовки переносятся на бэкенде, а не копированием в UI.** Заявку по шаблону
  собирает бэкенд: он же копирует `template.checklist` → `ticket.checklist`
  (сбросив отметки, сохранив `mandatory`) и кастомные поля. Клиент шлёт выбранный
  шаблон целиком, а не пересобирает его данные, — иначе легаси-форма заявки и
  форма шаблона разъедутся по правилам заполнения. Поля моделей приёмника и
  источника обязаны совпадать по именам, иначе Mongoose (strict) молча выбросит
  копию (проверять схему-приёмник перед тем, как класть в неё чужой объект).
- **Дочерний блок правится с карточки на месте** отдельным endpoint'ом — форму
  целиком ради одного чек-листа не открываем (см. «Страница сущности»).
- **Шаблон ≠ регламент, но регламент порождается из шаблона.** Шаблон — заготовка
  ручного создания заявки, регламент создаёт заявки сам по cron; «Создать
  регламент» в «⋯»-меню шаблона открывает форму регламента, предзаполненную этим
  шаблоном (`/routine-tasks/add?fromTemplate=…`, предзаполнение делает loader).

Текстовое описание (шаблон, регламент, заметка) везде правит **один редактор —
toastui из базы знаний** (`UI/MarkdownEditor` / `MarkdownViewer`), без
переключателя markdown/WYSIWYG (`hideModeSwitch`): второй редактор в приложении
не заводим, внешний вид приводим к макету обёрткой + scoped-CSS
(`.tpl-editor`/`.tpl-doc`), а не правкой сгенерированного тулбара.

---

## Страница отчёта и графики

Эталон — «Аналитика» (`pages/Report/Analytics.tsx`, первый мигрированный
отчёт; макет согласован до кода). Правила — для всех будущих отчётов
(финансовые и т.д.).

**Каркас** (`components/Report/ReportShell`; в `components/app` обобщим на
втором отчёте):

- Заголовок в языке `ListWrapper` (h1 + тулбар справа), но сам `ListWrapper`
  не используется — у отчёта нет поиска/сортировки/пагинации.
- Режимы отчёта — `Segmented` в тулбаре, активный несёт query
  (`?view=trends`) — канон «Архива»: у каждого режима свой zustand-стор
  (`store/reports/*`), ремоунт при переключении, параметры переживают уход со
  страницы. Гейт между режимами не нужен, пока права эндпоинтов одинаковые.
- **Период — `app/MonthStepper`, дефолт отчёта — текущий месяц** (живой
  взгляд на «сейчас»; у архива — прошлый, там текущий «ещё не история»).
  Произвольный период — в Sheet-фильтре (`Report/FilterSheet` — та же шторка,
  что у списков, с `InsideOverlayContext`); кнопка фильтра — `success`, когда
  период произвольный (`isFullMonthRange` из `util/period`). Кнопки
  «Сформировать» нет: открытие страницы сразу строит отчёт, смена любого
  параметра сама делает запрос.
- Сторы: seq-guard от гонок листания (поздний ответ старого периода не
  перетирает свежий); **ошибка не сбрасывает `data`** — устаревший отчёт
  остаётся под danger-баннером с «Повторить». Контракт UI:
  `isLoading && !data` — скелет раскладки, `isLoading && data` — приглушение
  `tw:opacity-60`.
- Экспорт (Excel/CSV) — outline-кнопка-меню в тулбаре; `xlsx` подгружается
  динамически в момент экспорта, в чанк страницы не входит.
- Тяжёлые страницы (recharts) — **lazy-роут** (`route.lazy` в `App.jsx`,
  первый — аналитика): чанк не грузится тем, кто отчёт не открывает;
  протухший после деплоя чанк закрывает `vite:preloadError` в `main.jsx`.

**KPI-плитки** — `app/StatTile` + `StatTileDelta`: значение периода + дельта
к **предыдущему периоду той же длины** (бэкенд отдаёт `totals` и
`prev.totals`, проценты считает фронт). Подпись базы сравнения («к прошлому
периоду») обязательна.

**Графики — только shadcn chart (recharts):**

- Каждый график — `ChartContainer` + `ChartConfig`, цвета **только**
  `var(--chart-N)`/токены. Хардкод-цветов (hex/hsl-радуг) в графиках не
  бывает. `components/ui/chart.tsx` сгенерён CLI (в условных классах
  индикатора тултипа точечно доставлен пропущенный префикс `tw:` — брак
  генератора; при перегенерации проверить).
- **Палитра `--chart-1..5`** (`styles/tailwind.css`): порядок слотов
  фиксированный — это механизм различимости при дальтонизме, не косметика;
  тёмная тема — свои затемнённые ступени тех же оттенков. Менять — только с
  прогоном валидатора dataviz-скилла против поверхностей `#fff`/`#1d2024`.
  Красного слота нет намеренно: статусные цвета (destructive/warning) не
  бывают рядовыми сериями.
- **Цвет закреплён за сущностью, не за рангом**: классы работ всегда
  Выезды = chart-1, Удалённо = chart-2, Регламент = chart-3
  (`Report/ClassLegend`); в «Динамике» слоты раздаёт базовый порядок компаний
  (по суммарному времени) — переключение метрики ничего не перекрашивает.
- **Форму выбирает задача**: одна величина — стат-плитка, не график; величина
  по категориям / состав целого — горизонтальный стек-бар
  (`Report/StackedTimeBars`); изменение во времени — линии. Двухосевых
  графиков не бывает; сумма и слагаемые на одном графике не смешиваются.
- **Серий больше пяти не рисуем**: хвост сворачивается в «Прочие» (в барах —
  серая полоса-хвост, в линиях — пунктир `--faint`); произвольный выбор
  серий — `ChipMultiCombobox`, максимум 5 (слоты палитры).
- Светлые слоты палитры на белом ниже контраста 3:1 ⇒ **подписи значений на
  графике либо таблица рядом обязательны** (у стек-баров — сумма у конца
  полосы через `LabelList`).
- **Hover обязателен**: `ChartTooltip`, контенты — **только**
  `ChartTooltipContent`/`ChartLegendContent` (div-разметка; сырые recharts
  `Legend`/`Tooltip` рисуют несброшенные ul/li — preflight выключен). Точки
  линий — только на наведении (`activeDot`), тултип сортируется по значению.
- **Высота графика — явная** (`tw:aspect-auto` + вычисляемый `style.height`):
  aspect-контейнер от нефиксированного родителя схлопывается в ноль в
  мобильном app-shell; у горизонтальных баров высота = строки × шаг.
- Оси и сетка рецессивные (`faint`/`border-soft`), у линий — без вертикальных
  линий сетки.

**Таблицы отчёта**: shadcn `ui/table` в панели с `tw:overflow-x-auto`
(страница никогда не скроллится горизонтально); сортировка — локальный state
таблицы (в стор не уезжает), «Итого» — в `TableFooter`; детализация —
раскрытием строки в bar-list (`Report/BarList`: одна величина — один оттенок),
а не отдельным аккордеоном.

**Карточки-метрики** (`Report/MetricCards`) — паттерн «сводка =
переключатель»: карточка показывает изменение метрики и спарклайн «Итого» и
одновременно выбирает метрику главного графика (активная — рамка `primary`).
Один элемент делает две работы — пять больших графиков легаси сжались в один.

---

## Формы → нижняя шторка

Это центральный паттерн приложения. Форма добавления/редактирования — **вложенный
маршрут** (`add`, `update/:id`) списка, который рендерится в **шторке,
выезжающей снизу** (десктоп — колонка 672px по центру, мобайл — почти весь
экран).

**Механика:**

- Состояние видимости — общий стор `store/offcanvas.js`
  (`isActive` / `setShow` / `setClose`); `layout/Root.jsx` сам открывает и
  закрывает шторку по хвосту пути (`add`/`update`/`schedule`/`confirm`).
  **Исключение:** формы базы знаний открываются в основной панели.
- Шторку с `<Outlet/>` рендерит `app/ListWrapper` (внутри — `app/FormSheet`);
  кнопка создания встроена в его шапку.
- **Клик по свободной области экрана не закрывает шторку формы на десктопе**
  (`app/FormSheet` гасит `onInteractOutside` при `!isMobile`) — защита от
  случайной потери данных в наполовину заполненной форме. Закрыть можно
  **крестиком**, **успешным сабмитом** или **`Escape`** (все идут через
  `onOpenChange`); `Escape` закрывает ВСЕ шторки приложения одинаково — его
  не гасить. На мобайле — поведение по умолчанию (тап по затемнению
  закрывает).
- **Ширина шторки на десктопе — под контент**: по умолчанию колонка 672px
  (`max-w-2xl`); большим формам со сводкой даём `wide` (`max-w-4xl`, 896px) —
  проп идёт `app/ListWrapper` → `FormSheet`. Мобайл — всегда почти весь экран.

**Оболочка формы — `components/app/FormWrapper`:**

- `fetcher.Form method="post"` (router-actions), сабмит без перезагрузки.
- Заголовок формы; кнопка закрытия — крестик самой шторки.
- Ошибки/успех — `AlertMessage` (`variant="danger"`/`"success"`).
- Кнопки справа внизу: **«Отмена»** (ghost) и **«Сохранить»** (primary,
  `disabled` пока идёт сабмит); на мобайле ряд липнет к низу шторки.
- При успешном сабмите шторка закрывается и происходит `navigate("..")`
  (или `successTo`).

**Раскладка полей внутри формы:**

- Каждое поле — **`app/Field`** (лейбл + `required`-звёздочка + контрол +
  `hint`); свитчи — **`app/SwitchField`** (подпись + подсказка-эффект + скрытый
  input `"true"/"false"` для FormData). Руками эти блоки не собираем.
- **Подпись контрола всегда связана с контролом** — `<label htmlFor>` либо
  label-обёртка: клик по подписи переключает свитч/чекбокс и фокусирует поле
  (площадь клика, тач, скринридеры). Каталог делает это сам: `Field` /
  `SwitchField` — из коробки, `SettingRow` — пропом `htmlFor` (обязателен для
  строк с одним контролом), `UI/Select` маппит `id` → `inputId` react-select
  (id на контейнере-div связь не даёт). Подпись-«div» рядом с контролом без
  связи — баг; связать не с чем (иконка-кнопка, ячейка матрицы «событие ×
  канал») — тогда `aria-label`.
- Выпадающие списки — `UI/Select` + скрытый `<input type="hidden" name=...>`,
  чтобы значение попало в `FormData` router-экшена.
- Два поля в ряд — `tw:grid tw:gap-3 tw:md:grid-cols-2`; узкие экраны
  схлопываются в один столбец.
- Длинную форму делим на секции `ui/Card` с заголовком — это способ «разбить»
  форму без модалок-вкладок.
- Даты — нативный `<input type="date">`; перед записью в стейт приводим к
  `yyyy-MM-dd`.

### Навигация после сабмита (куда попадает пользователь)

Правило трёх исходов — его нарушение дезориентирует («создал модель с
карточки типа — оказался на списке моделей»):

1. **Создание сущности, у которой есть карточка, → карточка созданной
   сущности**, а не список. `successTo` у `FormWrapper` принимает функцию от
   ответа action — она строит адрес из `_id` созданного объекта (значит,
   бэкенд-ответ обязан содержать созданную сущность; наши `add`-контроллеры
   уже так отвечают). Эталон — `pages/DeviceModel/Add.jsx`,
   `pages/DeviceType/Add.jsx`. У сущности без карточки (вендор, атрибут)
   исход по умолчанию — остаться на списке (`".."`).
2. **Редактирование → остаёмся там, где были.** Форма правки — вложенный
   маршрут той страницы, где её открыли: у списка — `update/:id` в его
   шторке, у карточки — `update` в шторке самой карточки (после сабмита
   `".."` возвращает на неё). Кнопка «Изменить» на карточке никогда не
   уводит на список (`components/DeviceModel/View.jsx`,
   `components/DeviceType/View.jsx`).
3. **Создание с «чужой» страницы → форма открывается вложенным маршрутом
   этой страницы.** «Новая модель» на карточке типа — это `models/add`
   на карточке типа (шторка на месте, контекст не теряется), а не переход
   на список моделей; после успеха — п.1, карточка созданной модели.

Механика в `app/FormWrapper`: по успеху `navigate(successTo ?? "..",
{ replace: true })` — запись формы не остаётся в истории, поэтому браузерное
«назад» с карточки созданной сущности возвращает на страницу, где открыли
форму создания. Дополнительно карточка даёт постоянный путь вверх по иерархии:
родительская сущность в hero — ссылка (тип устройства на карточке модели),
а не просто текст.

---

## Диалоги → только мелкие вещи

Модальные диалоги — для **подтверждений и коротких действий**, не для форм.

Эталон — **`components/app/DeleteItem.tsx`**:

- `AlertDialog` + `RouterForm method="post"`: заголовок — название удаляемого,
  текст — **«Вы уверены? Это действие нельзя отменить.»**, скрытый `id`,
  футер — «Закрыть» и **«Удалить»** (`destructive`, `intent="delete"`).
- Из «⋯»-меню диалог открывается **состоянием** (`DeleteDialog` рендерится вне
  radix-меню — меню размонтирует содержимое при закрытии), это уже собрано в
  `app/ListRow`.
- Диалог закрывается на сабмите: строка исчезнет с редиректом экшена, и
  открытый модальный radix оставил бы залипший `pointer-events` на `<body>`.

Если полей становится много или появляется логика в несколько экранов — это
уже не диалог, переносите в шторку/мастер.

**Не делать:** длинные формы создания/редактирования в диалоге; вложенные
диалоги; диалог ради того, что укладывается в шторку снизу.

---

## Большие многошаговые формы → горизонтальный прогресс

Для мастеров (несколько логических шагов) сверху показываем **горизонтальный
индикатор прогресса**, чтобы пользователь видел, где он и сколько осталось.
**tw-эталон** — `components/ServicePlan/Form.jsx` (мастер услуги, макет
согласован до кода).

**Когда дробить форму на шаги.** Если полей много и они распадаются на
**логические этапы** (у услуги: «Основное» · «Тарификация» · «График») — это
мастер, а не одна простыня. Форму разбиваем на шаги с валидацией по шагу и
кнопками «Назад»/«Далее»/«Сохранить»; механика во всех мастерах одна — массив
`STEPS`, `stepValid(i)`, `maxReached`, `attempted` (см. `ServicePlan/Form.jsx`).
Длинную, но **одно-логическую** форму мастером не притворяем — её достаточно
разбить на секции (см. «Раскладка полей» выше).

**Создание — визард, правка — плоская.** Один и тот же набор полей в создании
проходят по шагам (визард со сводкой), а в правке дают **одной простынёй секций
без степпера**: правят обычно одно поле, гонять его по шагам — лишний путь
(`isEdit ? <секции> : <визард>`; эталон — `TicketTemplate/Form.jsx`).
**Форма подстраивается под тип сущности.** Если от одного переключателя зависит,
применима ли половина полей, — не показывайте неприменимое серым, **убирайте шаги
целиком**. У пользователя тип аккаунта (сотрудник / клиент / служебный) решает
набор шагов: клиенту не нужны права, категории и финансы, служебному — почти
ничего. Взаимоисключающие флаги модели при этом собираются в **один сегмент**, а
обратно в флаги — при сабмите (`kindToFlags`), а не тремя свитчами в форме.

**Сложное вложенное тело — JSON, а не FormData.** Пока форма плоская, `UI/Select`
+ скрытый input достаточно. Как только в теле появляются вложенные объекты и
массивы (права по модулям, матрица уведомлений, список компаний), ручная сборка
из `FormData` становится источником тихих потерь — пропущенный ключ, опечатка в
имени поля, строка `"true"` вместо boolean (всё это было в форме пользователя).
Тогда форма собирает объект сама и шлёт `fetcher.submit(payload, { method:
"post", encType: "application/json" })`, а router-action просто пересылает тело.

**Длинные наборы флагов — по модулям с «рубильником».** Группа = панель с
мастер-свитчем в шапке и счётчиком «N из M»; выключенный модуль гаснет.
Зависимое поле живёт **внутри** своего права (категории заявок — под
«Выполнение заявок»), а не отдельной вкладкой.

**Один каталог на показ и на правку.** Набор прав/способностей описывается один
раз (`User/permissions-catalog.js`) и питает и форму, и карточку. Два списка
неизбежно расходятся: у формы не было трёх прав модели, а `permissions =
permissions` сбрасывал их при каждом сохранении.

**Чужие личные настройки правим с предупреждением.** Если админ меняет из формы
то, чем обычно управляет сам пользователь (уведомления): объект уходит на сервер
**только если его действительно трогали** — иначе сохранение затрёт чужой выбор;
рядом — предупреждение «перезапишете личный выбор»; бэкенд мержит значения по
путям, а не заменяет поддокумент целиком.

**Необязательный шаг можно пропустить** — на нём рядом с «Далее» стоит
«Пропустить» (шаг чек-листа в шаблоне и регламенте: заполнять не обязаны, а
сам блок потом правится с карточки).

**Степпер — `components/app/WizardStepper.tsx`** (tw, готовый):

- Ряд кружков 32px с номером; пройденные — `RiCheckLine`. Состояния токенами:
  текущий/пройденный — `bg-primary text-white`; будущий — `bg-accent text-faint`
  + `inset-ring`; подписи — `accent-text` / `muted-foreground` / `faint`.
- Между кружками — линия-коннектор (`bg-primary` для пройденного участка,
  `bg-border` для оставшегося).
- Клик по шагу — в режиме правки (`allowJump`) или по достигнутым (`maxReached`).
- **По умолчанию тянется на всю ширину контейнера** (`flex` + дети `flex-1`). На
  форме со сводкой это обязательно: степпер идёт **во всю ширину шторки**, а не
  обрывается над левой колонкой — иначе лента шагов смотрится оторванной от
  сводки. Нужен узкий — задайте `max-w-*` через `className`.

  ```jsx
  <WizardStepper
    steps={[{ label: "Основное" }, { label: "Тарификация" }, { label: "График" }]}
    current={step}
    maxReached={maxReached}
    allowJump={isEdit}
    onStepClick={handleStepClick}
  />
  ```

**Живая сводка справа — у большой формы-мастера обязательна.** Панель
(`components/ServicePlan/Summary.jsx`) наполняется по мере прохождения шагов
(незаданное — «—», разделы раскрываются по `maxReached`) и держит контекст:
название, категории, цена, график видны сразу, не листая шаги назад. Под сводку
шторку **расширяем** (`wide` у `ListWrapper`/`FormSheet` → `max-w-4xl`), колонки —
`tw:flex tw:flex-col tw:md:flex-row` (контент `flex-1`, сводка `md:w-72`).

**Прогресс-бар вместо степпера** — когда нужен процент выполнения/готовности,
а не дискретные шаги: тонкая полоса, над ней компактная подпись и счётчик
«N / M», вариант по состоянию (готово — `success`, в процессе — `primary`).

---

## Панели настроек: селектор → условный блок

Экран настроек с выбором внешнего сервиса строится по паттерну «селектор →
условный блок конфигурации» (живой пример — секция «Искусственный интеллект»
настроек системы, `components/Preferences/Ai.jsx`):

- **Мастер-переключатель** (свитч) включает раздел; пока он выключен, все поля
  ниже идут `disabled`.
- **Селект выбирает вариант** (провайдера) и раскрывает его блок настроек;
  у каждого варианта — свой набор полей: секреты — всегда input
  `type="password"`, специфичные поля добавляются точечно (например,
  «folder ID» у Yandex, помеченный `*`).
- **Динамический список «по ключу»** — когда варианты подгружаются от сервиса
  по введённому ключу: селект + кнопка обновления рядом (`RiRefreshLine`, на
  время запроса — спиннер). Плейсхолдер «— загрузите список —», уже
  сохранённое значение остаётся выбираемым (даже если его нет в свежем
  списке), ошибка загрузки — инлайн-текстом у поля.
- Если у варианта **нет API для перечня опций** — статический селект
  (фиксированный список отдаёт бэкенд) либо, когда каталог открытый или
  слишком большой, свободный ввод с подсказками через `<datalist>`
  (идентификатор модели вводят вручную).
- Это **контролируемая конфигурация**, которую сохраняют целиком одним
  объектом, — не router-action форма: селекты здесь нативные, без
  `UI/Select` + скрытого input.

### Секция внешнего сервиса обязана отвечать, работает ли она

Включённый свитч — это намерение, а не факт. Секция, которая ходит наружу
(почта, Telegram, интеграция), обязана иметь **строку состояния**
`app/HealthRow` и **действие проверки** — иначе единственный способ узнать,
что канал молчит, это логи контейнера. Эталон — секции «Сбор заявок» и
«Уведомления → почта (SMTP)».

- **Строка живёт постоянно, а не появляется после нажатия.** Её наполняют
  фоновые процессы (крон сбора почты, реальные отправки уведомлений), поэтому
  она честна и без единого клика; кнопка проверки лишь добавляет свежий факт.
  Хранилище — группа `health` рядом с настройками канала
  (`backend/services/mail/health.js`): `lastOkAt`, `lastMessageAt`,
  `lastError` + `lastErrorHint`, `lastErrorAt`, `consecutiveFailures`.
- **Фраза, а не бейдж** (см. «Статус, который протухает»): цветом красим только
  иконку и состояние, дату и детали — `muted`. Причину сбоя формулирует
  **бэкенд** (`describeMailError`: код ошибки → «что случилось» + «что
  делать»), фронт её не угадывает.
- **Проверка не блокирует сохранение**: сервер может лежать, а настройки
  записать уже нужно. Успешную проверку записываем в `health`, неуспешную —
  нет: проверяют обычно черновик, и красная строка про несохранённые поля
  врала бы о работающем канале.
- **Действие называет результат, а не механику**: у приёма — «Проверить»
  (`RiRefreshLine`), у отправки — «Отправить тестовое письмо»
  (`RiMailSendLine`), потому что успешный коннект к SMTP ещё не значит, что
  письмо примут. Адресат — текущий администратор, отдельного поля нет.
- **Включённый канал валидируется на бэкенде**: неполная конфигурация — это
  канал, который «работает» только на вид. Ответ 422 с человеческой фразой
  («Укажите сервер IMAP — без него письма не забрать») уходит в тост.

### Секреты в настройках

Пароли и API-ключи **наружу не отдаются**: `GET` подменяет значение пустой
строкой и ставит флаг `<поле>IsSet`, форма рисует `••••••••  (задан)`, пустое
поле при сохранении означает «не менять» (`helpers/preferencesSecrets.js`;
тот же канон у ключа PRO32 в `User/UserForm.jsx`). В базе — шифртекст
`services/crypto/secretBox`. Следствие для формы: список моделей и проверки
работают с пустым полем — бэкенд подставит сохранённый секрет сам.

### Поля, общие для двух каналов, — один компонент

Транспорт почты (сервер и порт, режим шифрования, учётная запись, доверие
самоподписанному сертификату) одинаков у приёма и отправки, поэтому его
рисует единственный `Preferences/MailChannelFields`, а различия приходят
пропсами. Дефолтные порты по режиму продублированы ровно в двух местах —
компоненте и `backend/services/mail/transport.js`; менять синхронно.

---

## Страница личных настроек («Мой аккаунт»)

Эталон паттерна «настройки» в новой системе — `pages/User/MyAccount.jsx`:

- **Одна страница вместо вкладок**: разделы — `app/SettingsSection`-панели
  подряд; на десктопе слева липкий `app/AnchorRail` (см. «Страница сущности»),
  на мобайле — просто секции.
- Строки настроек — `app/SettingRow` (название + hint + контрол справа).
- **Уведомления — матрица «событие × канал»** (shadcn Checkbox): 6 строк
  вместо двух списков свитчей. Скрытые глобальными настройками категории всё
  равно уходят в FormData скрытыми полями с текущими значениями — иначе
  сохранение молча сбрасывает их (закрытый баг легаси).
- Каждая секция-форма — свой `fetcher.Form` с `intent` на action страницы;
  результат — глобальный тост (`store/toast-store.showToast`).
- Тема — сегмент трёх состояний light/dark/system (`app/ThemeSegment` через
  `ThemeContext`; про перезагрузку при смене — см. «Тема и цвет»).
- **Фоновая картинка пользователя**: читаемость текстов канвы обеспечивает
  «лист» в `layout/Root.jsx` (см. раздел миграции), сами экраны про фон не
  знают.
- **Подтверждение внешнего события** (подключение Telegram-бота): после
  действия — revalidate-поллинг loader'а до смены статуса, с гейтом
  `useFetchers().every(f => f.state === "idle")` — ревалидация при живом
  fetcher роняет роутер («Did not find corresponding fetcher result»).
  Статус ожидания — текст с точкой `warning` и подсказкой, что сделать.

Бэкенд-контракты, на которые опирается экран (закреплены после починки):

- `updateMyAccount` **мержит** `notify` по путям (правило — в «Больших
  многошаговых формах») и не трогает `telegramBot` — привязка бота создаётся
  только ботом через `/api/tg/auth`.
- **Имя категории уведомления едино в трёх местах**: `prefs.notify.personal.X`,
  `user.notify.byTelegram.X`, `user.notify.byEmail.X` — так его читают
  `notifyTg`/`notifyEmail` в `middleware/notifications.js`. Новая категория =
  флаг в модели Preferences (+ свитч в админке) + поля в модели User + ветка
  `case` в цикле нотификаций; значение `lastAction` берётся из **enum схемы
  Ticket**.
- **Статика, читаемая на каждой странице** (фон рабочего стола), хранится
  **локально** (`uploads/`, multer `diskStorage`), не в S3 — иначе каждый вход
  в приложение оплачивается presigned-чтением. Аватары пока в S3 — кандидат
  на ту же схему.

---

## Словарь действий

Одно действие — одно слово, на всех поверхностях (кнопка, заголовок формы,
вкладка браузера, пункт «⋯»-меню). Расхождения ниже — не стиль, а баг. Канон:

| Ситуация | Канон |
|---|---|
| Правка структурной записи (инвентарь, компания, пользователь, заявка, услуга) | **«Изменить»** — в ⋯-меню, hero-кнопке, заголовке формы-шторки и `document.title`. Заголовок — глаголом: «Изменить модель устройства». |
| Правка свободного текста на месте (заметка, Obsidian-режим `NoteView`) | **«Редактировать»** — намеренное исключение: правим текст, а не поля записи. |
| Создание: кнопка-триггер, заголовок формы, `document.title` | **«Новый / Новая / Новое X»** (существительное, согласование рода): «Новая модель», «Новый вендор». Одно слово на всех поверхностях создания. Асимметрия с правкой намеренна: создаём — существительным («Новая модель»), меняем — глаголом («Изменить модель»). |
| Сабмит любой формы и инлайн-модалки | **«Сохранить»** (прогресс — «Сохранение…»). Глагол «Создать» как сабмит не используем. |
| Отмена формы/подтверждения (есть сабмит, меняющий данные) | **«Отмена»** — в т. ч. диалог удаления, модалки действий заявок, загрузка изображения. |
| Закрытие read-only / просмотра (нечего отменять) | **«Закрыть»** — просмотр вложения, оригинала письма, фото в полном размере. |
| Удаление | **«Удалить»**. «Удалить безвозвратно» — только для необратимого (минуя корзину). |
| Фильтр | **«Применить» / «Сбросить»** (без слова «фильтр» в тексте кнопки). |
| Возврат из карточки к списку | крошки **«‹ Раздел»** (не кнопка «К списку»). |
| Переход к связанной сущности | **«Открыть …»**: «Открыть карточку», «Открыть профиль». |
| `document.title` | Обычное предложение, **не капслоком**: «Изменить компанию», «Новая модель»; карточка — **«Просмотр X»**. |

**Военного сленга в интерфейсе нет.** «В строю», «на передовой», «бойцы» —
отвергнуто прямо. Про доступность людей пишем нейтрально: **«работают»**,
**«доступно»**, **«отсутствуют»**, **«не на работе»**.

**Исключение — генерация, а не добавление.** Действие, которое _порождает_ объект,
а не добавляет существующий (API-ключ, пароль), может называться
**«Создать» / «Сгенерировать»**. Такие места: создание API-ключа
(`Company/View/ApiKeysSection.jsx`) и **«Создать регламент»** на шаблоне заявки
(порождает регламент, предзаполненный этим шаблоном, — см. «Доменные блоки»).

**«Добавить X» — только привязка существующего.** Единственная роль глагола
«Добавить» — привязать уже существующую сущность к родителю (выбор из каталога/
списка), а не создать новую: «Добавить атрибут» (из каталога к типу), «Добавить
услугу» (подключить компании), «Добавить устройство» (из инвентаря в мониторинг),
«Добавить ответственного». Создание же — всегда «Новый / Новая / Новое X».

---

## Тема и цвет

Поддерживаются **светлая и тёмная** темы + «системная» (`store/theme-context.jsx`:
`theme` = `light | dark | system`, флаг `isDark`, сохраняется в `localStorage`).
Провайдер ставит класс **`.dark` на `<html>`** — от него живут все токены новой
системы (и `UI/Select`: он стилизован css-переменными и тему в JS не читает).
Легаси-CSS дополнительно переключают `layout/DarkTheme.jsx` /
`layout/LightTheme.jsx` — поэтому до эндшпиля миграции смена темы перезагружает
страницу.

**Палитра новой системы — токены** (`src/styles/tailwind.css`, значения для
светлой / тёмной):

| Токен | Значение | Когда |
|---|---|---|
| `primary` | бирюза `#00bc8c`, текст белый | главное действие («Сохранить», «Новый …»), одно на экран |
| `background` | `#fff` / `#1d2024` | **базовая поверхность** (шторки, диалоги, панели) — сток-семантика shadcn, НЕ канва |
| канва | `#f4f5f6` / `#16181c` | фон страницы; красится оболочкой (`#root`/`body`), не токеном |
| `card` / `popover` | как `background` | панель списка, меню, поповеры |
| `muted-foreground` / `faint` | два уровня приглушённого | мета-строки, подсказки / счётчики, плейсхолдеры, неактивное |
| `accent` | серый ховер | фон наведения строк и ghost-кнопок |
| `accent-text` | читаемая бирюза `#007a5e` / `#0bd39f` | акцентный текст: метки групп, включённые способности |
| `destructive` | `#d63c3c` / `#f05252` | удаление, ошибки, `required`-звёздочка |
| `success` / `warning` / `info` | `#00bc8c` / `#eb9c14` / `#4d94e8` | семантика статусов (варианты Badge/Alert/Button) |
| `border` / `border-soft` | тонкие границы | контуры панелей / разделители строк |

Принципы: **статус — цветной текст с точкой, а не заливной бейдж**; заливка —
только у главного действия и семантических алертов.

**Цвет — ресурс, а не украшение.** Правило, выведенное на редизайне базы знаний:
на экране цветом говорим **максимум о трёх вещах**. В базе знаний это тип
заметки, состояние проверки и опасное действие. Однородные сущности (категория,
компания, пользователь) различаются **иконкой** и живут одинаковой нейтральной
пилюлей с `Ri*`-иконкой, иначе бейджи превращаются в светофор без смысла.

---

## Иконки

- Только **react-icons / Remix (`Ri*`)**, размер задаём `size={...}`; lucide
  живёт только внутри сгенерированных `components/ui`. Единственное
  отступление — несколько `Tb*` в мобильном таб-баре
  (`layout/MobileBottomNavbar.jsx`) — легаси, заменить на `Ri*` при касании.
- Закрепившиеся соответствия: `RiSaveLine` — сохранить, `RiArrowGoBackFill` —
  закрыть/назад, `RiAddFill` — добавить, `RiDeleteBinLine` — удалить,
  `RiRefreshLine` — обновить/перегенерировать, `RiCheckLine` — выполнено.
- Иконка обычно идёт перед текстом в кнопке (`<RiSaveLine /> Сохранить`).
- На узких экранах подпись прячут, оставляя иконку (так делает кнопка
  создания в `ListWrapper`).

---

## Обратная связь и статусы

- **Тосты** — **sonner** (`ui/sonner`, `<Toaster/>` в `Root.jsx`, снизу
  справа): вызов — `showToast(variant, message)` из `store/toast-store.js`
  (bootstrap-имена вариантов маппятся на toast.success/error/… — контракт
  сохранён на время миграции; легаси-остатки `ToastContainer`/`UI/AlertToast`
  уходят с миграцией своих областей).
- **Инлайн-алерты** — `app/AlertMessage` (тоже принимает bootstrap-имена
  вариантов). Внутри форм ошибку/успех показывает сам `FormWrapper`.
- **Статусы в списках** — цветной текст с точкой или метка группы
  (`ListGroupLabel`); заливной бейдж — для компактных мест, карты
  «статус → вариант» собираем рядом с компонентом.
- **Длительные операции** — `app/Spinner`; для опроса фонового результата —
  паттерн «спиннер + поллинг».
- **Пустые состояния** — не пустой экран: в списках встроены в `ListWrapper`
  (см. «Анатомия списка»), в секциях карточек — см. «Страница сущности».

---

## Ошибки и гейты прав

Единый каркас — `components/Error/ErrorScreen.jsx`: код с котом-талисманом
вместо средней цифры (4·4, 4·3, 5·0, 5·3; состояние без HTTP-кода — кот соло),
заголовок, пояснение, действия, строка автоповтора и тихая тех-строка.
Состояния — `components/Error/{404,403,500,503,NetworkError}.jsx`, диспетчер —
`pages/Error.jsx` по `useRouteError`: 401 → редирект на `/auth`; 403; 404;
502/503/504 → «Обновляемся»; сетевой TypeError fetch («Failed to fetch») →
«Нет соединения»; всё прочее → 500.

- **Текст — «что случилось → почему могло → что делать»**: без «Упс»,
  капслока и вины пользователя. 404 называет сущность по имени и в правильном
  роде («Не нашли заявку №51713») — карта «маршрут → слово» в
  `components/Error/entity-context.js` (новый раздел = новая запись), главная
  кнопка ведёт к списку раздела, не абстрактно «На главную».
- **Оболочка выживает**: errorElement стоит на уровне детей корня (App.jsx) —
  навбар и таб-бар живые, из ошибки можно уйти в любой раздел; корневой
  errorElement — фолбэк на случай падения самого authDataLoader. Root узнаёт
  об ошибке через `store/route-error.js` (флаг ставит layout-эффект
  `pages/Error.jsx`) и кладёт страницу на канву (944) без легаси-Card и
  сайдбара.
- **503 и «Нет соединения» проверяют себя сами**: интервал 5 с / событие
  `online` → `use-availability-probe` пингует публичный `/api/app-version` и
  по успеху перезагружает страницу (после деплоя это заодно подтягивает свежие
  ассеты). Ревалидацией роутера ретрай не делаем — после ошибки она не
  гарантирует перезапуск упавших загрузчиков.
- **Анимации** (`tw:err-cat-settle`, `tw:err-pulse`) — в `styles/tailwind.css`
  рядом с row-flash, reduced-motion уважается. Кадрирование кота — по
  пиксельному bbox овала в `public/error-emoji.png` (264×307 из 424×564).
- **Гейт прав в шторке/секции — `components/Error/InlineForbidden.jsx`**:
  плитка-замок + «нужно право „X“» (label из `User/permissions-catalog.js`).
  Полноэкранный 403-арт внутри шторок не рендерим; полностраничный гейт —
  `Error/403.jsx`.

---

## Чек-лист перед мерджем UI

**Формы**

- [ ] Форма открывается в **нижней шторке** через маршрут `add`/`update` и
      обёрнута в `FormWrapper`. Исключение — документ: правится на месте.
- [ ] На десктопе шторку не закрывает клик мимо (`Escape` — закрывает, его не
      гасить); ширина под контент (`wide` — формам со сводкой).
- [ ] Навигация после сабмита: создание → карточка созданной сущности, правка →
      остаёмся где были; redirect с `replace`; форма с «чужой» страницы —
      вложенным маршрутом этой страницы.
- [ ] Поля — `app/Field`/`app/SwitchField`; выпадающие списки — `UI/Select` +
      скрытый input.
- [ ] Каждая подпись связана со своим контролом (клик по подписи переключает
      свитч/чекбокс, фокусирует поле): `Field`/`SwitchField`/`SettingRow
      htmlFor`/label-обёртка; подписей-«div» без связи нет.
- [ ] Диалог — только подтверждение/короткое действие.
- [ ] Мастер: степпер во всю ширину шторки, живая сводка справа; создание —
      визард, правка — плоская; неприменимые типу шаги убраны целиком.
- [ ] Вложенное тело (объекты, массивы, матрицы) — **JSON**, не ручной FormData.
- [ ] Чужие личные настройки уходят на сервер только при реальном изменении, с
      предупреждением; бэкенд мержит по путям.

**Списки**

- [ ] Заголовок секции, скрытой правами, не рендерится; данные видны без
      обязательного фильтра; пустое состояние предлагает действие.
- [ ] Серверная выборка: счётчик — `total` от API (`count`), пагинация в
      `belowList`, скоуп и поиск считает БД, фоновые обновления мержатся в
      строки без сброса страниц.

**Карточка**

- [ ] Одна залитая кнопка, выбранная состоянием; остальное — ghost/outline и
      icon-only «⋯»; подписи «Действия» нет.
- [ ] Запрос, ожидающий решения, — инлайн-`Alert` с автором и датой, не пункт
      меню.
- [ ] Статус с автором/датой/сроком — фразой, а не голым бейджем; бэкенд отдаёт
      имена (`populate`), а не `ObjectId`.
- [ ] Длинный однородный список — через `PillPanel` (сам сворачивает, «Показать
      все (N)»).
- [ ] Правка на месте (документ): геометрия чтения и правки совпадает,
      `Ctrl+E`/`Ctrl+S`/`Esc`, `useBlocker` + `beforeunload`, побочный эффект
      сохранения назван до сохранения.
- [ ] Информация, общая с другими разделами (чек-лист, кастомные поля,
      расписание), — одним компонентом из `components/app`; заготовки
      шаблон → заявка переносятся на бэкенде (поля моделей совпадают по именам).

**Мобайл**

- [ ] Панели — флекс/absolute-дети `.mobile-shell` (не `position:fixed`),
      скролл только у `.mobile-shell__scroll`, слушателей скролла на `window`
      нет; на экране один плавающий объект.
- [ ] Заблокированное массовое действие объясняет причину (тултип на десктопе,
      тап-подсказка на мобилке) и называет объекты по имени, не по id.

**Код, цвет и текст**

- [ ] Мигрированный экран: ни одного импорта react-bootstrap и
      bootstrap-классов; утилиты `tw:*` только из сетки (без `-[Npx]`).
- [ ] Повторяемый кусок — из каталога `components/app`, не свёрстан заново
      (перед вёрсткой — свериться с таблицей каталога).
- [ ] Иконки — `Ri*`; семантика — вариантами/токенами; цветом максимум о трёх
      вещах; статус в списке — текст с точкой/меткой группы, а не светофор
      бейджей.
- [ ] Тексты действий — по «Словарю действий» («Новый X» / «Изменить» /
      «Сохранить» / «Отмена» vs «Закрыть» / «Добавить» только для привязки).
- [ ] Графики — только `ChartContainer`+`ChartConfig` с токенами `--chart-*`
      (палитра — через валидатор), контенты тултипа/легенды — shadcn'овские;
      подписи значений или таблица рядом; серий >5 нет — хвост в «Прочие»;
      высота графика явная (см. «Страница отчёта и графики»).
- [ ] Проверено в светлой и тёмной теме, на десктопе и мобайле; верификация —
      `pnpm build` + `pnpm eslint`, живой UI проверяет Алексей (см. правила
      миграции).

---

## Журнал изменений

Обратная хронология; детали каждой записи разложены по тематическим разделам.

- **2026-07-26** — **Статусы присутствия стали автоматическими; режимы учёта
  времени и версии графика.** Календарь жил отдельно от статусов: «отпуск» мог
  выбрать себе кто угодно, а вечером табло показывало полный офис.
  **Правило, которое стоит помнить: семантика статуса живёт в каталоге
  (`utils/workStatuses.js`), а не в списках кодов по кодовой базе.** Знание
  «кто на связи», «кого можно к клиенту», «что показывать группами» было
  размазано по шести захардкоженным массивам, и новый код статуса **ронял
  сотрудника из вида „Сегодня"** — единственного места, где это не деградация,
  а исчезновение. Теперь у статуса есть `kind` (working · break · away · idle),
  `manual` и `visit`, а все шесть списков — производные.
  Новый статус **`offshift` «не на работе»** 🌙 (токены `--ws-st-offshift` в обе
  темы). Автоматика (`services/workStatusAuto`, крон 5 минут): начало смены →
  «в офисе» (у `remoteOnly` → «на удалёнке»), окончание → «не на работе».
  **Ручной выбор держится до конца дня.** Кто поставил статус, помнит поле
  `workStatus.auto`: автоматический меняется свободно, ручной живёт до конца
  суток (вечером гаснет только то, что не трогали после конца смены, — выход в
  субботу так сохраняется). Сначала правило было временны́м, без поля, и этого
  не хватило: отменённое в тот же день отсутствие неотличимо от ручного выбора,
  и статус застревал до утра.
  **Смена режима учёта сбрасывает автоматический статус на сабмите формы**:
  «свободный» и «не ведётся» автоматики не имеют, и «в офисе» от прежнего
  графика врал бы с той же секунды. Ручной при этом остаётся — его выбрал
  человек. Расписание, перерыв и производственный календарь показываются
  только режиму «по графику»; свободному остаются «только удалённо», часовой
  пояс и «действует с».
  **Согласованное отсутствие меняет статус сразу**, в момент решения
  (`runWorkStatusAuto({ userIds })` из контроллера), а не в ночном прогоне —
  иначе согласовали отгул, а человек до утра висит «в офисе». Отгул и дни без
  содержания получили общий статус **`absent` «отсутствует»** 🚫: тип называет
  заметка («отсутствует · Отгул»), потому что сам код молчит. Отсутствие
  сильнее графика, но слабее сегодняшнего ручного выбора — форс-мажорный
  больничный от админа живёт сутки, как и любой ручной статус.
  Отсутствия и «не на работе» руками не ставятся:
  `canSetStatusManually` пускает их только в свободном режиме и обладателю
  права «Графики и отсутствия»; UI такие пункты **не показывает**, потому что
  action «Мой аккаунт» намеренно глотает ошибки и 403 утонул бы молча.
  **Режим учёта времени** у сотрудника: «по графику» · «свободный» (в календаре
  есть, статусы ставит сам) · «не ведётся» (в календаре нет — подрядчики,
  разовые работы) плюс модификатор «только удалённо».
  **Календарь ходит вперёд** (`MonthStepper allowFuture`): отчёты и архивы
  смотрят в прошлое, а отпуска планируют на будущее. Год без изданного
  производственного календаря считается по дню недели, и это **обязательно
  подписано в интерфейсе** — январь без новогодних выходных выглядит рабочим,
  и отпуск спланируют не туда (`calendar.missingYears` в ответе API).
  **График ведётся версиями** с «действует с»: правка задним числом разрешена,
  но предупреждает, какие месяцы пересчитаются. Версия выбирается **на каждый
  день**, а не один раз на период: смена графика запросто попадает внутрь
  месяца. Новое в каталоге: **`app/Combobox`** — поле формы на Popover +
  Command; в шторке react-select рисует меню инлайном, и его обрезала прокрутка
  (список типов отсутствия было не пролистать). Остальные `UI/Select` меняем
  перед релизом.
- **2026-07-25** — **«Календарь команды»: доступность, производственный
  календарь и отсутствия** (по согласованному макету, ред. 3). Отчёт
  «Сотрудники» считал переработки по окну обслуживания клиента в единственном
  поясе организации: у инженера из UTC+10 обычная смена 09:30–17:00 целиком
  попадала в «до 09:00 по Москве» и числилась переработкой (7,5 ч на ровном
  месте). Появились производственный календарь РФ (`xmlcalendar.ru` →
  `isdayoff.ru` → снимок в репозитории), личный график с часовым поясом у
  сотрудника и отсутствия с согласованием; ядро — `services/workCalendar`,
  переработки по-прежнему считает единственный `services/workOvertime`.
  **Главный урок — про форму, и он стоил двух отвергнутых редакций макета.**
  Сначала ячейка дня была 24-часовой микрошкалой в 28 px (час в один пиксель —
  «я нихуя не вижу»), потом столбиком выработки. Обе редакции отвечали не на
  тот вопрос: **календарь измеряет ЛЮДЕЙ, а не часы**. Нормы, отработанного и
  переработок на нём нет — они живут в отчёте «Сотрудники»; два часа работ за
  день ничего не говорят о человеке и мешают увидеть главное.
  Календарь отвечает на четыре вопроса: кто сегодня работает и кому кидать
  заявку, кто болеет, кого можно физически отправить к клиенту, когда
  безопасно ставить отпуск. Отсюда три вида: **Месяц** — сетка 7×N, в ячейке
  по точке на сотрудника с **фиксированной позицией** (по горизонтали читается
  день, по вертикали — «этот в отпуске всю неделю»); **Сегодня** — группы по
  живому статусу с местным временем человека и пометкой «к клиенту» (в офисе и
  на выезде — можно, удалёнка может быть и из другой страны); **Планирование** —
  единственный вид с осью сотрудников, отсутствия полосами и строка «Доступно»
  с подсветкой провалов. Решение по запросу показывается **с цифрой**:
  «если подтвердить — останется работать 6 из 10».
  **Палитра календаря — существующие токены присутствия `--ws-st-*`**: он и бар
  статусов обязаны говорить одним языком, второй палитры не заводим. Знание о
  прошлом и будущем не смешивается: сегодня точка красится живым
  `user.workStatus`, дальше — планом (график + подтверждённые отсутствия).
  **Правило масштаба плотной сетки:** перед вёрсткой считайте пиксели на
  единицу данных; меньше 3–4 px на минимально значимую разницу — форма выбрана
  неверно, и лечится это сменой формы, а не увеличением.
  Новое в каталоге: **`app/ScheduleView`** — read-двойник `ScheduleEditor`
  (до него недельная сетка была свёрстана заново в трёх местах).
  Секция «График работы» на карточке сотрудника правится **на месте отдельным
  запросом** (`POST /users/:id/work-schedule`), как чек-лист шаблона; сотрудник
  запрашивает отпуск/отгул/больничный в «Мой аккаунт → График и отсутствия»,
  там же отзывает и читает причину отказа. В отчёт «Сотрудники» добавлены
  колонки «Норма» и «Δ»; строка без личного графика помечена янтарным
  «нет графика» — у неё переработки пока считаются по-старому.
- **2026-07-25** — **Почтовые каналы в настройках: гибкий транспорт, проверка,
  строка состояния** (по согласованному макету). Секции «Сбор заявок» и
  «Уведомления → почта (SMTP)» перестали быть настройкой одного сценария: были
  зашиты порт 993, TLS, папка INBOX и логин, равный адресу ящика, а у SMTP —
  единственный свитч `secure` и обязательная авторизация. Теперь режим
  шифрования — сегмент **SSL/TLS · STARTTLS · Нет** (порт подставляется по
  режиму, введённый руками не затирается), появились папка, доверие
  самоподписанному сертификату и релей **без авторизации** (логином ящика
  по-прежнему служит его адрес); сборка конфига — единственное место
  `services/mail/transport.js` (точная копия в telegram-bot, он отправляет).
  Новое в каталоге: **`app/HealthRow`** — постоянная строка состояния внешнего
  канала, её пишут крон сбора, реальные отправки бота и кнопки «Проверить» /
  «Отправить тестовое письмо» (`POST /preferences/mailbox/check`,
  `/preferences/smtp/test`, оба под rate-limit); причины формулирует бэкенд
  (`describeMailError`), проверка сохранение **не блокирует**. Поля транспорта
  обеих секций рисует общий `Preferences/MailChannelFields`; `formatAgo`
  переехал из `Mikrotik/meta` в `util/format-date`, у `app/Segmented` появился
  `disabled`. **Секреты** (пароли почты, все AI-ключи) больше не уезжают в
  браузер и лежат шифртекстом `secretBox` — маска + «пусто = не менять»
  (`helpers/preferencesSecrets.js`), ключ читается как
  `APP_ENC_KEY || MIKROTIK_ENC_KEY`. Починено попутно:
  `validations/preferences.js` был мёртвым (не подключён, поле `htmlDesc`) —
  переписан под частичное сохранение и включён, инварианты включённого канала
  отвечают 422 с человеческой фразой; сборщик получил гард «нет инициатора по
  умолчанию → заявки не создаём» (инвариант «заявка без company»), защиту от
  «ядовитого письма» (3 попытки → `\Seen`) и перестал звать `findById`
  с пустым `defaultCompany._id`; мёртвый `backend/middleware/nodemailer.js`
  удалён. Миграция — `scripts/migrateMailSettings.js` (идемпотентна,
  `isSecure` → `security` с учётом порта); деплой — три сервиса.
- **2026-07-25** — **«Персональный» и «По сотрудникам» слиты в раздел
  «Сотрудники»** (по согласованному макету). Отчёты отвечали на один вопрос, а
  «По сотрудникам» вдобавок считал переработки и премии **мимо канона**:
  захардкоженный график 09:00–18:00, tariffingPeriod 15 и ставка 1000 ₽/20 мин
  вместо графиков тарифов и индивидуальных ставок. Теперь: `/finances/employees`
  — сводная (часы, заявки, классы работ, переработки, доплата; строка ведёт в
  отчёт сотрудника, **своя помечена «Вы»** и видна даже без работ),
  `/finances/employees/:userId` — его отчёт, `/finances/my-report` — свой
  (страница одна, отличия — заголовок и крошки); старые адреса редиректят с
  сохранением `?userId=`. Меню: один пункт вместо двух — «Сотрудники» при
  полном праве, иначе «Мой отчёт». **Персональный отчёт стал самодостаточным**
  (сотрудник без права на аналитику не должен ходить за статистикой в другой
  отчёт): добавлены регламентные работы отдельным классом, разрез по категориям
  заявок, динамика за 12 месяцев; **согласование видно в двух местах** — панель
  статусов и колонка в списке работ; финансовые блоки (расчёт + согласование)
  стоят выше статистики. Бэкенд: новый `GET /finances/employees-summary`
  (`canSeeGlobalFinancialReport`, `approvedOnly`, лимит 366 дней) на
  `services/employeesSummaryService`; расчёт переработок — общий
  `services/workOvertime`, поэтому строка сводной **сходится с персональным
  отчётом того же сотрудника** (проверено инвариантом по всем метрикам).
  Найдено на реальных данных: у части сотрудников есть переработки, но не
  задана ставка — раньше это было невидимо (всем считалась одна), теперь
  янтарная пометка в строке и инлайн-предупреждение с именами и суммой.
  Легаси (`pages/Finances/PersonalReport.jsx`, `EmployeeReport.jsx`,
  `components/Finances/Personal/*`, `POST /employee-report`) удаляется после
  живой проверки.
- **2026-07-25** — **ядро агрегации работ** `services/workSummary` +
  `services/workOvertime`: одна классификация (регламент/выезд/удалёнка), одна
  формула длительности (с клэмпом битых отрицательных) и одни переработки на
  всё приложение. На них переведены аналитика, дашборд, статистика компании и
  персональный отчёт — до этого формула жила в ~7 копиях, а переработки в 3.
  Тренды строятся одним запросом вместо ~390 (было по запросу на компанию ×
  период). Побочно закрыта дыра прав (`create-invoice`/`confirm-payment`/
  `delete` были доступны любому пользователю финмодуля) и починена «Моя
  статистика» на главной: запрос заявки шёл по несуществующему полю
  `work.ticketId` → номер и категория были пусты, инициатор — «undefined
  undefined». Эквивалентность доказана эталонами ответов до/после (15 срезов
  байт-в-байт).
- **2026-07-25** — **«Аналитика» мигрирована** (по согласованному макету) —
  первый отчёт на целевой системе и пилот правил нового раздела «Страница
  отчёта и графики». Одна страница `?view=` с сегментом «Сводка | Динамика»
  вместо селектора типов; кнопка «Сформировать» упразднена — открытие сразу
  строит текущий месяц (`app/MonthStepper`, произвольный период —
  Sheet-фильтром). Сводка: KPI-ряд `app/StatTile` (вынесен из «Активности»
  компании, та переведена на него) с дельтами к прошлому периоду той же длины
  — бэкенд `getCompanySummary` отдаёт `totals` + `prev.totals` одной лёгкой
  доп. выборкой; классификация работ сведена в общий `summarizeWorks`;
  **вложенный populate `routineTask` в prev-выборке обязателен** — сырой
  ObjectId удалённого регламента классифицировал работу иначе, чем основной
  цикл (ловилось инвариантом `A.prev.totals === B.totals`). Один
  горизонтальный стек-бар «Время по …» (топ-10 + «Прочие») заменил пару
  легаси-диаграмм с радугой `hsl(i*137.5)`; таблицы с сортировкой, «Итого» и
  раскрытием строки в bar-list (вместо аккордеона с пирогами); разрез
  «Компании | Сотрудники» сегментом под KPI; клиентский вид — подразделения
  (subdivisionStats получили счётчики классов). Динамика: карточки метрик со
  спарклайнами = переключатель единственного графика (вместо пяти легаси),
  топ-5 компаний + пунктирные «Прочие», выбор компаний комбобоксом (≤5 —
  слоты палитры), таблица периодов с Δ; из ответа trends удалены никем не
  читаемые `executors` (и populate `finishedBy`). Оба эндпоинта переведены
  **POST→GET** (идемпотентное чтение, канон `/api/works`), границы периодов —
  `$lt` эксклюзивной полуночи, битые даты → 400. Инфраструктура: **recharts 3
  + `ui/chart.tsx`** (CLI; точечный фикс непрефикснутых условных классов),
  палитра `--chart-1..5` пересобрана и провалидирована в обеих темах (красный
  слот заменён розовым — статусные цвета не серии; тёмные ступени под тёмную
  поверхность), **первый lazy-роут** (чанк аналитики ~405KB отдельно от
  главного бандла), `MIGRATED_ROUTES` + 1328, первые `.ts`-сторы
  (`store/reports/*`) и `src/vite-env.d.ts`. Легаси (`components/Reports/*`,
  старый `pages/Report/Analytics.jsx`, сирота `WorkReport.jsx` + мёртвые
  бэкенд-роуты `filterWorks`/`getFormData`) удаляются после живой проверки.
  Таймзоны сводки (UTC-полночь vs бизнес-TZ на стыках месяцев) — осознанный
  follow-up одним заходом с `/api/works`; тренды уже на dayjs.tz.
- **2026-07-24** — **Верхняя навигация реорганизована, «Архив» объединил
  заявки и работы** (по согласованному макету). Меню: «Компании» и
  «Пользователи» — всегда верхний уровень при любом праве доступа
  (`canPerformTickets || canManage* || isAdmin`; из «Администрирования»
  ушли — ежедневные страницы больше не меняют место от роли), «Мониторинг» —
  прямая ссылка на борд Mikrotik (дропдаун из одного пункта упразднён),
  «Отчёты» похудели (остались Аналитика и финансовые), «Администрирование» —
  три группы с uppercase-заголовками по модулям («Основное / Финансы / Учёт
  техники», «Расположения» — в учёте техники), «Настройки системы» — в меню
  аватара и футере NavDrawer (только isAdmin), «Dashboard» → «Главная» (как в
  мобильном таб-баре), «Архив» — последним пунктом в обоих деревьях. Форма
  группы раздела — `{ label?, items }` (рендеры: `SectionDropdown` в
  Navbar + NavDrawer). Ширина бара на xl — статичные `shortLabel`
  («Люди», «База», «Админ»), полные названия в дропдаунах/drawer.
  «Диапазоны сетей» — тихая ссылка в тулбаре мониторинга (страница пока
  легаси). **Страница «Архив»** (`/archive`; постоянные редиректы
  `/closed-tickets → /archive`, `/report/work → /archive?view=works`):
  сегмент «Заявки | Работы» (`?view=works`; гейт сегмента = гейты
  легаси-отчёта: модуль учёта времени + canUseTimeTrackingModule +
  canSeeWorksReport, в т.ч. для конечных пользователей; deep-link без права
  тихо падает на «Заявки», сегмент из одного пункта не рендерится) — два
  компонента с двумя `ListWrapper` (`components/Ticket/ArchiveList` +
  `components/Work/ArchiveList`), ремоунт при переключении восстанавливает
  поиск из стора, фильтры сегментов живут в своих сторах
  (`store/lists/closed-tickets` и `store/lists/works`). Сегмент «Работы» —
  бывший легаси-«Отчёт по работам» на канве архива: `GET /api/works`
  (`work.getFinished` — зеркало `ticket.getClosed`: страницы по 50, поиск по
  описанию/исполнителю/№ заявки, фасеты компании/категории/исполнители,
  populate без N+1 легаси; `totalDurationMs` по всей выборке агрегатом →
  строка «Суммарное время выборки» в новом слоте `aboveList`; отрицательные
  длительности битых данных в сумму идут нулём), обязательных «компания +
  период» больше нет — список открыт сразу, ≥ 12 ч — warning-текст с точкой.
  Скоуп прав: end-user принудительно заперт в своей компании (закрыта дыра
  легаси `filterWorks`, бравшего company из body без проверки); в
  `tickets/form-data` конечным пользователям добавлен каталог категорий
  (фасет был пуст в обоих сегментах). Индексы works: `{finishedAt,_id}` и
  `{company,finishedAt}`. Найдена порча данных: у ~34 работ битый
  `startedAt` (вплоть до ~2000 лет длительности, 3 отрицательных) — глобальная
  сумма без фильтров искажена ими; кандидат на разовый скрипт чистки в
  `backend/scripts/`. Легаси (`pages/Report/WorkReport.jsx`,
  `report.filterWorks`/`getFormData` и их роуты) удаляются после живой
  проверки. **Доводка по живой проверке**: в «Администрировании» группа
  «Основное» → «Заявки», пункт «Категории заявок» → «Категории»; «Отчёты» —
  короткие имена + заголовок группы «Финансы» (Аналитика │ ФИНАНСЫ:
  Согласование · Персональный · По сотрудникам — слово «отчёт» не повторяем,
  оно в названии раздела); период архива — видимым чипом-степпером
  `app/MonthStepper` в тулбаре обоих сегментов, дефолт и «Сбросить» — прошлый
  месяц (`util/period.js`; текущий месяц ещё «не в архиве»), «Суммарное время
  выборки» показывается **только при заданном периоде**; «Диапазоны сетей»
  переехали из строки инструментов мониторинга в ряд полосы RouterOS
  (правый край; тулбар был перегружен — «слишком скученно»). **Меню получило
  модульные гейты, зеркальные API** (исторический пробел, вскрылся при
  выключении модулей — раньше эти пункты гейтились только правами):
  финансовые пункты («Согласование/Персональный/По сотрудникам», «Услуги»)
  — `modules.finances.isActive + canUseFinancesModule` (весь `/finances`
  смонтирован за `financesModuleIsActive + canUseFinancesModule`,
  routes/index.js), группа «Учёт техники» «Администрирования» —
  `modules.inventory.isActive + canUseInventoryModule` (зеркально монтажу
  `/inventory`).
- **2026-07-24** — **«Мониторинг Mikrotik» мигрирован целиком** (по
  согласованному макету): список `/devices/mikrotik` — статус-борд парка на
  `ListWrapper` с группировкой по статусу («Не в сети» → «В сети» →
  «Мониторинг выключен», заголовки исчезают при единственной группе), в каждой
  строке — **лента доступности за 30 дней** (окна по 24 ч; API списка отдаёт
  `uptimeDays`, считает `computeUptimeStats` по эпизодам) и длительность
  текущего офлайна; полоса RouterOS сжата в чипы веток с числом отстающих
  (чейнджлог — правой шторкой); клик по строке — шторка-превью (статус +
  ошибка + авто-заявка эпизода, CVE, свойства, «Открыть устройство»); тихий
  поллинг 15 с и deep-link `?recordId=`/`?clientDeviceId=` сохранены. **У
  каждой записи — своя страница** `/devices/mikrotik/records/:id` (раньше —
  только у CHR): hero по канону карточки, секции «Подключение» (PropRow),
  «Прошивка и безопасность» (полный список CVE), «Доступность» (периоды
  сегментом, стат-плитки, лента, журнал простоев с заявками), «Сеть»,
  «Конфигурации» (расписание правится на месте, 2FA-скачивание без изменений;
  секция видна только с правом configs). **Добавление — только «Новое
  устройство»** (решение из записи об интеграции выполнено): шаг выбора из
  инвентаря и пул notConfigured удалены, форма едина для всех (CHR перестал
  быть особым случаем), сабмит — «Сохранить» с подписью «Перед сохранением
  проверим подключение»; после проверки — шаг «Устройство подключено» с
  блоком **«Инвентарь»: авто-связь по серийному номеру** (нашлась карточка —
  «Связать карточку», нет — «Создать карточку» при включённом модуле и праве
  на устройства; `POST records/:id/link-inventory` / `create-inventory`,
  связь питает live-статусы «Окружения»/«Техники»). Бэкенд стал
  record-центричным (`GET/POST/DELETE …/records/:id`, connect/disconnect);
  старые эндпоинты по `clientDeviceId` остались легаси-вкладке «Мониторинг»
  карточки устройства инвентаря — она мигрирует вместе с инвентарём, её
  компоненты (`components/Devices/Mikrotik/*`) живы, новые — в
  `components/Mikrotik/*`. Отчёт «Диапазоны сетей» в редизайн не входил.
  `POST /preferences/update-db-conf`: кнопка выполняла одноразовые бэкофиллы
  эпохи 1.8.x (users→employees, embedded→refs у заявок, comment.ticketId),
  давно завершённые в данных, и грузила при этом всю коллекцию заявок.
  Миграции данных делаются разовыми скриптами в `backend/scripts/` (паттерн:
  `backfillUserLastActivity`, `encryptGetScreenKeys`), а не вечной кнопкой в
  настройках. Хвост из ~141 почтового комментария-сироты без `ticketId`
  осознанно не тронут — при желании чинится отдельным скриптом.
- **2026-07-24** — **PRO32 Connect обустроен в «Интеграциях»**: подгруппа с
  заголовком, свитч «Удалённое подключение к клиентам» с честным описанием
  (сессию создаёт пользователь своим API-ключом — заводится в форме
  пользователя) и блок **«Подключённые пользователи»** (`GET
  /users/pro32-connected`) с отзывом доступа (`POST /users/pro32-revoke/:id`
  стирает ключ; AlertDialog-подтверждение, тост с именем). **Ключи
  зашифрованы** как у Mikrotik (`services/crypto/secretBox`, AES-256-GCM):
  запись шифрует, читатели (`helpers/getScreenKey.js`) понимают и старый
  плейнтекст (дошифруется при сохранении; разовый
  `scripts/encryptGetScreenKeys.js` — уже прогнан в dev), наружу ключ не
  отдаётся — `getOne` маскирует в `getScreen.hasApi`, поле формы: пусто =
  «не менять», очистка — только отзывом из настроек. На «Пользователях» —
  фасет Sheet-фильтра «Подключён PRO32 Connect» (`?pro32=true` в getAll;
  фасет виден при включённой интеграции) со снимаемым бейджем; карточка
  показывает статус по `hasApi`.
- **2026-07-24** — **Mikrotik стал самостоятельной интеграцией**: отдельная
  секция настроек упразднена — теперь подгруппа в «Интеграциях» с
  мастер-свитчем «Мониторинг и управление устройствами Mikrotik»
  (`prefs.mikrotik.isActive`; отсутствие поля в старых документах = включено).
  Свитч тушит всё разом: меню («Мониторинг», «Диапазоны сетей» — флаг едет в
  `preferences-initial` → `store/prefs`), API (middleware `mikrotikIsActive`
  вместо модульных `inventoryModuleIsActive`+`canUseInventoryModule`; путь
  `/inventory` сохранён) и кроны бэкенда (`services/mikrotik/enabled.js` —
  ранний выход в health-check, scheduler, offline-alerts, firmware). Прежняя
  привязка к модулю «Учёт техники» была дырявой: выключение модуля прятало
  UI/API, а кроны продолжали работать. Отложено до редизайна
  `/devices/mikrotik`: добавление устройств только созданием нового (без
  выбора из инвентаря) — с решением про авто-связь с инвентарной карточкой.
- **2026-07-24** — **правило «Подпись контрола всегда связана с контролом»**
  (см. «Раскладка полей внутри формы») — по багу UX в настройках: клик по
  подписи свитча ничего не делал. Аудит всех мигрированных экранов показал:
  прежняя миграция дисциплинированна (label-обёртки/`htmlFor` везде — фильтры,
  чек-лист, график, диалоги), нарушали только новые секции настроек. Починено
  системно: `SettingRow` получил проп `htmlFor` (название → `<label>`), всем
  контролам секций настроек розданы `id`; `UI/Select` теперь маппит `id` →
  `inputId` react-select — связь `Field htmlFor + Select id` ожила во всех
  формах приложения (раньше id висел на контейнере-div и клик по лейблу
  селекта был инертен).
- **2026-07-24** — **«Настройки системы» мигрированы** (по согласованному
  макету): bootstrap-вкладки → одна страница с рейлом-якорем (канон «Мой
  аккаунт» — `SettingsSection`/`SettingRow`/`AnchorRail`), заголовок выровнен
  с пунктом меню. **Сохранение по секциям**: каждая панель — свой
  fetcher-сабмит JSON + «Сохранить» в подвале (`Preferences/SectionForm`),
  бэкенд `preferences.update` переведён на **частичное обновление** — меняет
  только присланные группы (`hasOwnProperty`-гарды; сохранение одной секции не
  перезаписывает чужие правки), `notify.byTelegram` мержится **по полям**.
  **Группа Telegram — единая** (подгруппа «Канал: Telegram» секции
  «Уведомления»): `notify.byTelegram.chatId` + новая общая ветка
  `messageThreadId` обслуживают и групповые уведомления (message_thread_id
  прокинут через очередь `Notification.to` до `tgSendMessage` бота), и табло
  статусов (свитч `statusBoard.isActive` — там же); дубль `statusBoard.chatId`
  удалён из схем обоих сервисов, смена группы/ветки инвалидирует закреп табло
  (бот пересоздаёт), `/status_board` работает только в настроенной группе
  команды и задаёт общую ветку. **Повторы при ошибке отправки убраны из
  настроек вовсе** (`notify.global` выпилен из схем): интервал и число попыток
  — инженерные константы бота (`telegram-bot/utils/retryPolicy.js`), а не
  решение админа — пример правила «настройка не заменяет решение
  разработчика»; секции модулей (База знаний,
  Переработки, Mikrotik) видны только при включённом модуле и не оставляют
  пункт в рейле, «Учёт финансов» в UI блокируется без «Учёта времени» (как и
  на бэкенде). AI и SMTP — по канону «селектор → условный блок» (выключенный
  блок гасит поля); «Переработки» переведены с легаси `UI/Schedule` на общий
  `app/ScheduleEditor`; лого — прежними отдельными эндпоинтами; мёртвый
  экспорт `initialPrefsLoader` удалён. Ответ бэкенда — «Настройки сохранены»
  (тост); часовой пояс после сохранения дублируется в localStorage, как
  раньше.
- **2026-07-24** — **«Архив заявок» мигрирован и переведён на серверную
  выборку** (по согласованному макету). Раньше список не показывал ничего,
  пока не заполнены обязательные «Период» и «Компании» (анти-паттерн «фильтр
  открывает данные»), и грузил всю выборку разом; теперь открывается сразу:
  «Сначала недавние», по 50 на страницу (`app/Pager` — вынесен из
  `User/Pager` в каталог, «Пользователи» переведены на него), серверный поиск
  с дебаунсом (номер — точным совпадением, тема, описание, ФИО инициатора —
  предзапросом в User: embedded `applicant` у старых заявок пуст). Фасеты —
  Sheet (период двумя нативными полями даты, компании/инициаторы/
  ответственные/категории мультиселектами по каталогу
  `form-data?includeInactive=true`) + чип «Компании» в тулбаре; применённое —
  снимаемые бейджи. Строка read-only: № (↻ у регламентных) · тема + мета ·
  ответственные · «закрыта/создана»; клик — заявка в новой вкладке; статуса
  нет — в архиве всё «Закрыта». Бэкенд: `POST /tickets/closed` →
  `GET /tickets/closed` (`page`/`limit`/`search`/`from`/`to`/фасеты/`sort`;
  `find().lean()` + `countDocuments`, ответ `{tickets,total,page,limit}`),
  скоуп прав выровнен с `getRecentlyClosed` (canSeeAllCompanyTickets теперь
  жёстко своя компания — раньше ветка пропускала любые; «участвовал» =
  responsibles/createdBy/applicantId), индекс `{isClosed, finishedAt, _id}`
  обслуживает листинг и обе сортировки без in-memory sort. У
  `ListWrapper`/`SearchBar` появился проп `searchPlaceholder`.
- **2026-07-23** — **страницы ошибок** переработаны по согласованному макету:
  единый `ErrorScreen` (кот — средняя цифра кода), контекстные 404 («Не нашли
  заявку №…», `entity-context.js`), новые состояния 502/503/504
  («Обновляемся», автопроверка каждые 5 с) и «Нет соединения» (повтор по
  `online`), вложенный errorElement — оболочка выживает (Root ←
  `store/route-error`), компактный `InlineForbidden` в шторках инвентаря,
  тех-строка «код · путь». Причина «500 вместо 404» устранена: загрузчик
  заявки перестал глотать ошибки, битые ObjectId в URL переводятся в 404
  глобально (backend `middleware/errorHandling.js`). Удалены `css/error.css`,
  Montserrat, капслок и мёртвая ветка 402. Детали — в «Ошибки и гейты прав».

- **2026-07-23** — **секция «Техника»** (`app/TechSection`) заменила секцию
  «Окружение» на карточках компании и пользователя: одни данные в двух
  представлениях («Список | Окружение»). Список — строки с жёсткими колонками
  и live-точкой Mikrotik на плитке; фасеты страницы «Устройства» чипами со
  снимаемыми бейджами; лимит 10 строк и «Вся техника в „Устройствах" (N) →»
  с префильтром query (стор и легаси-фильтр устройств получили фасет
  «Закреплено за»). Техника пользователя — тремя источниками с группами
  («Личная и рабочее место», «В помещении — X» уровнем выше). По
  согласованному макету v2; детали — в «Переиспользуемых доменных блоках».
- **2026-07-23** — **«Окружение» переведено на целевую систему и стало общим
  доменным блоком** `app/Environment` (+ плитка и шторка устройства; старые
  `Ticket/View/Environment*` и `css/environment.css` удалены). По
  согласованному макету: линейка глубины с бирюзовой точкой-гало, сцена-«план»
  с точечной сеткой, картографический кластер «+/−» вместо текстовых кнопок,
  статусы техники текстом с точкой, шторка устройства как превью расположений,
  пустые состояния по канону карточек. Виджет встроен секциями «Окружение» в
  карточку пользователя (`subject="user"`, между «Организацией» и «Заявками»)
  и карточку компании (после «Структуры»; вход — обзор корневых расположений,
  новый endpoint `GET /locations/company/:companyId/environment` со
  счётчиками по поддереву; подпись «Здания» честно деградирует в
  «Расположения», когда в корне «висячие» помещения/РМ). Детали — в
  «Переиспользуемых доменных блоках».
- **2026-07-23** — **лог активности компании** переработан по согласованному
  макету v2 (radix Sheet снизу вместо bootstrap-Offcanvas). Иерархия — от
  главного вопроса «за каким компьютером сидит пользователь»: поиск первой
  строкой (карту фильтрует мгновенно на клиенте, журнал — серверно с
  дебаунсом) → карта «Кто за каким компьютером» (агрегат-эндпоинт
  `GET …/logs/accounts`: свежие имя/логин, компьютер и время последнего
  входа, число входов) → «Журнал входов» с «Показать ещё». Связь
  «AD-учётка ↔ пользователь» управляется прямо в строке карты ОДНИМ действием
  на учётку (связь живёт на GUID; раньше кнопка размазывалась по каждой
  строке журнала и читалась как действие над компьютером): связанная — тихая
  иконка отвязки, несвязанная — warning-пометка + «Связать». Бэкенд-поиск
  журнала переписан с «$lookup всего журнала + пайплайн дважды ради count» на
  индексный find с предзапросом пользователей и экранированным термом. Модель
  `CompanyLog` получила `firstName`/`lastName` из AD (шлёт агент). Мёртвый
  `CompanyLogsSection` удалён.
- **2026-07-23** — **формы компании** мигрированы по согласованному макету —
  bootstrap-мост `Company/FormOffcanvas` удалён (список вернул встроенную
  шторку `ListWrapper` c `wide`, карточка открывает правку в `FormSheet`).
  Создание — мастер «Основное · Контакты · График работы» со сводкой
  (`Company/FormSummary`), правка — плоская секциями (канон «создание —
  визард, правка — плоская»); телефонов теперь несколько (динамический список
  «Ещё телефон», бэкенд add/update принимает массив), домены — строка через
  запятую (сплитит бэкенд, пустые отбрасываются), «со стороны клиента» —
  только в правке. `ScheduleEditor` переехал из `ServicePlan/` в
  `components/app` (его делят услуга и компания). Сабмит — JSON; создание
  уводит на карточку созданной компании.
- **2026-07-23** — **переключатель активности компании** (по образцу
  пользователя): «Отключить/Включить» в «⋯»-меню карточки
  (`Company/ToggleActiveDialog`), hero-статус «Отключена» (destructive, живой
  график скрыт); в списке — свитч «Только активные» (дефолт on) в
  Sheet-фильтре + бейдж «С отключёнными», строка приглушается, вместо графика
  — тихое «отключена» (негативное в списках — приглушённо, красное — только
  hero). Отключение каскадит денормализованный `user.company.isActive`
  (фильтры — только `{ $ne: false }`): гаснут вход (login + isAuth), выдачи
  пользователей («Только активные» в списке людей = активен сам И компания),
  выпадашки форм (`/api/companies` и `tickets/form-data` по умолчанию отдают
  только активные), машинные каналы (почта/телефония/API-ключи/
  саморегистрация) и плановые cron-регламенты. Исключения: Архив заявок
  (`?includeInactive=true`) и отчёты (читают снапшоты). Компанию по умолчанию
  для входящих заявок отключить нельзя — 409 + тост.
- **2026-07-23** — **карточка компании** мигрирована по согласованному макету:
  вкладки → один скролл с рейлом (`max-w-5xl`), hero с живым статусом графика
  (`WorkStatusText` получил `verbose`/`halo`) и счётчиками охвата; «Активность»
  — статборд-плитки вместо карточек статистики; секция графика подсвечивает
  сегодняшний день полоской прошедшей части дня; структура — tw-дерево со
  шторкой-справкой (как превью расположений) и диалогами формы/состава;
  сотрудники — адресная книга с «Все сотрудники (N) →» в раздел
  «Пользователи»; услуги и API-ключи — строками с диалогами; «Лог активности»
  и «Удалить» — в «⋯»-меню. В каталог вынесены `PropRow` и `SubLabel`
  (карточка пользователя переведена на них), у `Eyebrow` появился `action`.
  Бэкенд `getOne` отдаёт `updatedBy` (populate) и `customerApprovalRequired`
  услуг. Формы компании — по-прежнему легаси-мост. Тогда же (по отдельному
  макету) диалог «Добавить услугу» получил вторую ветку — «Новая услуга»:
  параметры подключения (дата, согласование) общие для обеих веток и уезжают
  query-строкой в прежний мастер услуги, открытый вложенным маршрутом карточки
  (`/companies/:id/service-plans/add`, wide-FormSheet); сводка мастера
  показывает блок «Подключение», бэкенд `servicePlan.add` принимает
  `attachCompany` и создаёт+подключает одним запросом (конфликт категорий
  проверяется до создания — услуг-сирот нет); после сохранения — возврат на
  карточку компании (осознанное отступление от «создание → карточка созданной
  сущности»: задача пользователя — настройка компании). Подсветку row-appear
  строке секции не давали: эти анимации — для полноширинных строк списков, в
  панели карточки вспышка выглядит чужеродно. Плитки «Заявки» и «Время работ»
  листаются по месяцам пейджером в `action` Eyebrow (`‹ июль 2026 ›`; вперёд —
  не дальше текущего, назад — не раньше создания компании): прошлые месяцы
  догружаются `GET …/stats?month=YYYY-MM` (бэкенд считает только помесячную
  часть, окно сравнения прошлого месяца — вся его длина) и кэшируются на время
  жизни карточки; «Активные пользователи» и «Основной канал» от месяца не
  зависят.
- **2026-07-22** — редактура гайда: журнал перенесён в конец, дубли сведены в
  канонические разделы, каталог сжат; сверка с кодом — дописаны `ChipSelect`,
  `ScheduleBuilder`, `AppBanner`, миграция «Регламентных заданий», сводный
  статус миграции. Тогда же из гайда вычищены **легаси-конвенции bootstrap**
  (палитра вариантов, легаси-карточка, легаси-анатомия списка, запрет
  `text-info` — он касался только бутстрап-тем): гайд описывает целевую
  систему, bootstrap упоминается только в правилах миграции.
- **2026-07-20** — строка списка «Компании» переверстана «жёсткими колонками»:
  логотип-плитка (`Company/CompanyLogo`), живой график «тихим форматом»
  (`WorkStatusText`, `util/get-working-status.js` `detail`), гнездо действий,
  такси за глобальной настройкой (`Preferences.taxi.operator`,
  `util/taxi-operators.js`, `Company/company-links.js`) → раздел
  «Строка-запись».
- **2026-07-19** — список «Компании» мигрирован («справочник клиентов»):
  сегмент «Все | Мои», Sheet-фильтр ответственных, мобильная шторка-справка
  `Company/ContactSheet`; карточка и формы — легаси через мост
  `Company/FormOffcanvas`.
- **Пользователи** — раздел мигрирован целиком; список — адресная книга на
  серверной выборке (эталон), карточка с `AnchorRail`, форма-визард с набором
  шагов по типу аккаунта, каталог прав `User/permissions-catalog.js`,
  JSON-сабмит.
- **Регламентные задания** — раздел мигрирован; `app/ScheduleBuilder` +
  `util/cron`; «Создать регламент» из шаблона (`?fromTemplate=`).
- **Карточка вендора** — модели свёрнуты в индекс по типам
  (группы-аккордеоны) → пример в «Когда список перестаёт помещаться»; чип-фасет
  «Тип устройства» заменён заголовками групп.
- **Шаблоны заявок** — первый экран на доменных блоках (`Checklist`,
  `CustomFieldsEditor`/`View`, `ChipMultiCombobox`); появился раздел
  «Переиспользуемые доменные блоки»; `Panel`/`Eyebrow` вынесены в каталог.
- **Словарь действий** — терминология сведена в единый раздел, тексты по всему
  UI приведены к канону.
- **Расположения** — раздел мигрирован: дерево `Location/Tree.jsx` с
  предпросмотром (`Location/PreviewSheet`), обязательный контекст-комбобокс
  `ChipCombobox`, `Segmented` получил `stacked`+`icon`; карточка с полной
  цепочкой предков. Тогда же — правило «Навигация после сабмита» (три исхода)
  и карточка вендора по языку карточек.
- **Услуги** — первая tw-карточка (`ServicePlan/View.jsx`) и мастер со сводкой
  (`ServicePlan/Form.jsx`) — эталоны; `FormSheet` перестал закрываться кликом
  мимо/Escape на десктопе. Тогда же (Фаза 3) — малые справочники «Типы» и
  «Атрибуты устройств»: `ListWrapper` + Sheet-фильтр + `activeFilters` +
  `ChipSelect`.
- **Бар статусов сотрудников** — `User/WorkStatusBar.jsx` на tw: rail
  72/336px, группы цветом статуса, футер «Обновлено …» от max(`updatedAt`) (не
  от «сейчас»), мобильная лента с чипами; палитра — токены `--ws-st-*`.
- **Оболочка** — навбар, тосты (sonner), меню пользователя, бургер
  `NavDrawer` — на tw (Фаза 2).
- **Пилот миграции** — «Вендоры» (списки) и «Мой аккаунт» (настройки);
  заведён каталог `components/app` и инфраструктура `tw:`/shadcn.
- **База знаний (до миграции)** — Obsidian-правка на месте, статус-фразы,
  «Бюджет управляющих элементов», «цвет — ресурс».
