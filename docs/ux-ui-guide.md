# UX/UI Guide

_Последнее обновление: 2026-07-02. Документ описывает UI-конвенции, как они
сложились в текущем коде, чтобы новые экраны выглядели и работали единообразно.
Это снимок и свод правил, а не строгая спецификация — при расхождении с кодом
ориентируйтесь на код и обновляйте этот файл._

## Стек и базовые принципы

- **React + react-router** (data router: `loader`/`action`, `useFetcher`,
  вложенные маршруты).
- **react-bootstrap** (Bootstrap 5) — основа всех компонентов. Layout строим на
  `Row`/`Col`, `Stack`, `Container`, отступы — bootstrap-утилитами (`mb-3`,
  `gap-2`, `ms-auto` …), а не кастомным CSS.
- **react-icons**, набор **Remix (`Ri*`)** — единственный источник иконок
  (`RiSaveLine`, `RiAddFill`, `RiDeleteBinLine`, `RiRefreshLine`, `RiCheckLine` …).
- **zustand** для UI-состояния (`store/offcanvas.js`, `store/toast-store.js` …),
  **Context** для темы (`store/theme-context.jsx`).
- **react-select** только через обёртку `UI/Select.jsx` (она тема-зависимая и
  рендерит меню в портал) — голый `react-select` не использовать.
- Отдельные ветки **`BrowserView` / `MobileView`** (`react-device-detect`) там,
  где десктоп и мобайл расходятся по раскладке.
- Весь UI-текст — **на русском**. Семантику передаём bootstrap-вариантами
  (`primary`/`success`/`danger`/`secondary`…), а не произвольными цветами.

---

## Когда что использовать (главное правило выбора контейнера)

| Ситуация | Контейнер | Референс |
|---|---|---|
| Подтверждение, короткий вопрос, мелкий диалог (1–3 поля) | **Modal** (`centered`) | `components/DeleteItem.jsx` |
| Заполнение/редактирование формы (add/update сущности) | **Offcanvas снизу** (`placement="bottom"`, `h-100`) | `UI/ListWrapper.jsx` + `UI/FormWrapper.jsx` |
| Большая многошаговая форма (мастер) | Offcanvas/страница + **горизонтальный степпер / прогресс-бар** | `components/ClientDevice/WizardStepper.jsx`, `components/Ticket/View/AiGuide.jsx` |
| Боковые фильтры списка (мобайл) | Offcanvas сбоку | `UI/ListWrapper.jsx` |
| Список/таблица как «всплывающая» панель | Offcanvas снизу | `layout/Dashboard/Offcanvas.jsx` |

Коротко, четыре опорных правила:

1. **Modal — только для маленьких диалогов**: подтверждения, единичный вопрос,
   быстрое действие. Не размещать в модалке полноценные формы.
2. **Заполнение форм — в Offcanvas, выезжающем снизу.**
3. **Большая многошаговая форма — с горизонтальным прогресс-баром/степпером**
   (как в `ClientDevice/Form.jsx` и `Ticket/View/AiGuide.jsx`).
4. **В тёмной теме не использовать текст и иконки цвета `info`** — низкий контраст,
   плохо читается (см. раздел «Тема и цвет»).

---

## Раскладка страницы

`layout/Root.jsx` задаёт каркас:

- **Десктоп** (`BrowserView`): фиксированная `Navbar` сверху (`fixed-top`), контент
  с `paddingTop: 100px`; основная область — `Card.shadow` с `<Outlet/>`, слева
  опционально колонка-сайдбар (`leftSidebarContent`) для контекстной навигации
  (списки тикетов, база знаний и т. п.). `Container fluid`, `maxWidth: 1920px`,
  горизонтальные отступы `px-5`. Переходы оборачиваем в `animations/Transition`.
- **Мобайл** (`MobileView`): нескроллящийся app-shell — см. раздел
  «Мобильная навигация».
- Десктоп и мобайл разведены через `BrowserView`/`MobileView` (`react-device-detect`,
  по User-Agent).
- Тосты — в `ToastContainer position="bottom-end"` (см. «Обратная связь»).

Списочные страницы строятся через **`UI/ListWrapper.jsx`**: заголовок, кнопки
«Обновить»/«Назад»/«Добавить» (`UI/AddButton.jsx`), поиск (`SearchBar`),
сортировка (`Select`), счётчик «Найдено: N», пустое состояние
(`AlertMessage variant="light"` — «Список пуст»).

---

## Мобильная навигация (app-shell)

На мобайле (`MobileView` в `layout/Root.jsx`) весь UI — **нескроллящийся app-shell**
`.mobile-shell` (`position:fixed; height:100dvh; display:flex; flex-direction:column;
overflow:hidden`). Скроллится только средняя область `.mobile-shell__scroll` (`<main>`);
верхний бар и нижний таб-бар — её соседи по флексу, **а не `position:fixed`**. Это
структурно убирает баг iOS Safari, где фиксированные панели «всплывали» посреди экрана
при инерционном скролле. Десктоп (`BrowserView`) сохраняет прежнюю схему (`fixed-top`
`Navbar` + `paddingTop:100px`).

```text
.mobile-shell                         position:fixed, 100dvh, flex-column, overflow:hidden
├─ <NavigationBar embedded />         верхний бар: .navbar.mobile-shell__header (в потоке)
├─ <main .mobile-shell__scroll>       единственный скролл-контейнер
│    └─ <Container> <Outlet/> <Footer/> </Container>
└─ <MobileBottomNavbar />             плавающий «остров»: .mobile-tabbar (absolute)
```

**Нижний таб-бар — `layout/MobileBottomNavbar.jsx`** («плавающий остров»):

- Скруглённый бар с `backdrop-filter: blur`, парит над краем
  (`position:absolute; bottom: calc(10px + env(safe-area-inset-bottom))`).
- Вкладка = иконка (`Ri*`/`Tb*`) + короткая подпись; тач-таргет ≥ 44px.
- Активная вкладка — бирюзовый акцент (`--bs-success`) и пилюля, которая **плавно
  скользит** между вкладками через `framer-motion` `layoutId` + `LayoutGroup`
  (с уважением `useReducedMotion`). Пилюлю центрируем через `inset`, а **не**
  `transform`: framer сам управляет `transform`, иначе выделение «съезжает».
- Набор вкладок — конфиг-массив, фильтруется по правам/`isEndUser` (как в меню).
  Активность считается явно; маршруты взаимоисключающие → ровно одна активная
  вкладка (инвариант: одна пилюля на `layoutId`).
- Обязательны `aria-label` и `aria-current` на вкладках (иконки — `aria-hidden`).

**Верхний бар (мобайл)** — тот же `NavigationBar` с пропом `embedded`: статичный
флекс-ребёнок (`.navbar.mobile-shell__header`) вместо `fixed-top`, с
`safe-area-inset-top` и нижней границей. Гамбургер + логотип + триггер меню.

**Бургер-меню (offcanvas)** — редизайн только в мобильной (`embedded`) ветке,
десктопный бар не затронут: шапка с аватаром-инициалами и ролью, крупные скруглённые
строки (`.drawer-nav`), бирюзовая подсветка активного пункта, футер с сегментным
переключателем темы + «Мой аккаунт» + «Выйти», safe-area-паддинги.

**Правила (важно):**

- **Не вешать слушатели скролла на `window`** на мобайле — он не скроллится (гейт
  `isBrowser` в `UI/BackToTop.jsx`); нужен скролл — слушать `.mobile-shell__scroll`.
- Панели навигации на мобайле — **никогда не `position:fixed`** (только флекс/absolute
  внутри shell), иначе вернётся «дрейф».
- Сброс скролла при навигации — вручную (`mobileScrollRef` + `useEffect` по
  `location.pathname` в `Root.jsx`; router `ScrollRestoration` не используется).
- Тема навигации — scoped-токены `--nav-*` / `--tab-*` / `--tabbar-*` (light в
  `index.css`, dark в `css/dark-theme.css`), акцент завязан на `--bs-success`.
  Контекст `navbar-dark` красит `.nav-link` белым — цвета ссылок меню перекрываем
  `!important`.
- `index.html`: `viewport-fit=cover` + `theme-color`; safe-area учитывается и сверху,
  и снизу.

---

## Формы → Offcanvas снизу

Это центральный паттерн приложения. Форма добавления/редактирования — **вложенный
маршрут** (`add`, `update/:id`) списка, который рендерится в **Offcanvas,
выезжающем снизу**.

**Механика (готовая, переиспользуйте, не изобретайте заново):**

- `UI/ListWrapper.jsx` рендерит в конце списка нижний Offcanvas с `<Outlet/>`:

  ```jsx
  <Offcanvas
    show={offcanvas.isActive}
    onHide={() => { navigate(-1); offcanvas.setClose(); }}
    keyboard
    placement="bottom"
    className="h-100"
  >
    <Offcanvas.Header closeButton />
    <Offcanvas.Body>
      <Outlet />
    </Offcanvas.Body>
  </Offcanvas>
  ```

- `UI/AddButton.jsx` ведёт на маршрут `add` и вызывает `offcanvas.setShow()`.
- `layout/Root.jsx` сам открывает/закрывает Offcanvas, ориентируясь на хвост
  пути (`add`/`update`/`schedule`/`confirm`). **Исключение:** формы базы знаний
  открываются в основной панели, а не в Offcanvas.
- Состояние видимости — общий стор `store/offcanvas.js`
  (`isActive` / `setShow` / `setClose`).

**Оболочка формы — `UI/FormWrapper.jsx`.** Оборачивайте в неё содержимое формы,
чтобы получить единообразные заголовок, обработку ошибок и кнопки:

- `fetcher.Form method="post"` (router-actions), сабмит без перезагрузки.
- Заголовок `<h1>{title}</h1>` + `<hr/>`, ниже — поля (`children`), снизу — `<hr/>`.
- Ошибки/успех — через `AlertMessage` (`variant="danger"`/`"success"`).
- Кнопки в `Stack direction="horizontal"`, прижаты вправо (`ms-auto`):
  **«Закрыть»** (`variant="secondary"`, `RiArrowGoBackFill`) и **«Сохранить»**
  (`variant="primary"`, `RiSaveLine`, `disabled` пока идёт сабмит).
- При успешном сабмите Offcanvas закрывается и происходит `navigate("..")`.

**Раскладка полей внутри формы** (эталон — `components/ClientDevice/Form.jsx`):

- Группируйте поля по смыслу в **`Card`-секции** с `Card.Header` → `<h6>`
  («Компания и расположение», «Устройство», «Покупка» …). Это и есть способ
  «разбить» длинную форму без модалок-вкладок.
- Два поля в ряд — `Row` + `Col md={6}`; узкие экраны схлопываются в один столбец.
- Каждое поле — `Form.Group className="mb-3"` с `Form.Label htmlFor=...`.
- **Обязательное поле** помечаем звёздочкой: `<span className="text-danger">*</span>`.
- Подсказки под полем — `Form.Text className="text-muted"`.
- Выпадающие списки — `UI/Select` + скрытый `<input type="hidden" name=...>`,
  чтобы значение попало в `FormData` router-экшена.
- Даты — нативный `<Form.Control type="date">`; перед записью в стейт приводим к
  `yyyy-MM-dd`.

---

## Modal → только мелкие диалоги

Модальные окна — для **подтверждений и коротких действий**, не для форм.

Эталон — **`components/DeleteItem.jsx`**:

- `Modal show centered`, внутри `RouterForm method="post"`.
- `Modal.Header closeButton` + `Modal.Title` (название удаляемого объекта).
- `Modal.Body`: текст подтверждения — стандартно
  **«Вы уверены? Это действие нельзя отменить.»** + скрытый `id`.
- `Modal.Footer`: **«Закрыть»** (`secondary`) и целевое действие
  (`variant="danger"`, `RiDeleteBinLine` для удаления).
- Триггер — `Dropdown.Item` (в меню действий) или `Button` (`isButton`).

Так же оформлены действия над тикетом (`components/Ticket/Actions/*` — Reject,
Close, TakeToWork, UpdateDeadline …): один шаг, пара полей, кнопка действия. Если
полей становится много или появляется логика в несколько экранов — это уже не
модалка, переносите в Offcanvas/мастер.

**Не делать:** длинные формы создания/редактирования в Modal; вложенные модалки;
модалку ради того, что укладывается в Offcanvas снизу.

---

## Большие многошаговые формы → горизонтальный прогресс

Для мастеров (несколько логических шагов) сверху показываем **горизонтальный
индикатор прогресса**, чтобы пользователь видел, где он и сколько осталось.

**Степпер — `components/ClientDevice/WizardStepper.jsx`** (готовый компонент):

- Ряд кружков 32px с номером шага; пройденные показывают `RiCheckLine`.
- Состояния цветом:
  - пройден — `bg-success text-white`, подпись `text-success`;
  - текущий — `bg-primary text-white`, подпись `fw-semibold text-primary`;
  - будущий — `bg-secondary-subtle text-secondary`, подпись `text-muted`.
- Между кружками — линия-индикатор (`bg-primary` для пройденного участка,
  `bg-secondary-subtle` для оставшегося).
- Клик по шагу разрешён в режиме редактирования (`allowJump`) или по уже
  достигнутым шагам (`maxReached`).

  ```jsx
  <WizardStepper
    steps={[{ label: "Основное" }, { label: "Сеть" }, { label: "Итог" }]}
    currentStep={step}
    maxReached={maxReached}
    onStepClick={setStep}
  />
  ```

**Прогресс-бар — `ProgressBar` из react-bootstrap** (живой пример —
`components/Ticket/View/AiGuide.jsx`): когда нужен процент выполнения/готовности,
а не дискретные шаги.

- Тонкая полоса (`style={{ height: "0.4rem" }}`), `now={progress}`.
- Над ней — компактная подпись и счётчик «N / M».
- Вариант по состоянию: `progress === 100 ? "success" : ...`.

> ⚠️ В `AiGuide.jsx` для незавершённого прогресса исторически стоит
> `variant="info"`. В тёмной теме это плохо читается — для новых экранов берите
> `primary` (см. следующий раздел).

Длинную, но **одношаговую** форму многошаговой не делаем — достаточно разбить её
на `Card`-секции, как в `ClientDevice/Form.jsx`.

---

## Панели настроек: переключатель + условные блоки

Экраны настроек с выбором внешнего сервиса (эталон —
`components/Preferences/Ai.jsx`, раздел «Искусственный интеллект») строятся по
повторяющемуся паттерну «селектор → условный блок конфигурации».

- **Мастер-переключатель** `Form.Check type="switch"` включает раздел; пока он
  выключен, все поля ниже идут `disabled`.
- **`Form.Select` выбирает вариант** (провайдера) и раскрывает его блок настроек
  через `{provider === "..." && (<>…</>)}`. У каждого варианта — свой набор
  полей: секреты всегда `Form.Control type="password"` (API-ключи), а
  специфичные поля добавляются точечно (например, «folder ID» у Yandex,
  помеченный `*`).
- **Динамический список «по ключу»** — когда варианты подгружаются от сервиса по
  введённому ключу: `Form.Select` + кнопка обновления рядом (`RiRefreshLine`, на
  время запроса — `Spinner`). Плейсхолдер «— загрузите список —», уже
  сохранённое значение остаётся выбираемым (даже если его нет в свежем списке),
  ошибка загрузки — инлайн `Form.Text className="text-danger"`.
- Если у варианта **нет API для перечня опций**, вместо динамического поля —
  либо обычный `Form.Select` со статическим набором (как у Yandex SpeechKit и
  YandexGPT; бэкенд отдаёт фиксированный список), либо, когда каталог открытый
  или слишком большой, свободный ввод `Form.Control type="text"` с подсказками
  через `<datalist>` (как у Yandex AI Studio — идентификатор модели вводят
  вручную).
- Подсказки под полями — `Form.Text className="text-muted"`.
- Эти панели используют **нативный `Form.Select`**, а не обёртку `UI/Select`:
  это контролируемая конфигурация, которую сохраняют целиком одним объектом, а
  не router-action форма (там, наоборот, обязателен `UI/Select` + скрытый input).

---

## Тема и цвет

Поддерживаются **светлая и тёмная** темы + «системная» (`store/theme-context.jsx`:
`theme` = `light | dark | system`, флаг `isDark`, сохраняется в `localStorage`).
CSS-наборы переключают `layout/DarkTheme.jsx` / `layout/LightTheme.jsx`;
пользовательский переключатель — `components/User/AccountSettings/Appearance.jsx`.
Кастомные компоненты, рисующие вне bootstrap-классов (например `UI/Select.jsx`),
обязаны читать тему и подстраивать стили.

**Палитра (Bootstrap-варианты):**

| Вариант | Когда |
|---|---|
| `primary` (фирменный тёмно-синий `#2c3e50`, акцент `#3498db`) | основное действие («Сохранить»), активный шаг, ключевые акценты |
| `success` | успех, завершённость, положительный статус; акцент активной вкладки мобильной навигации |
| `danger` | удаление, ошибки, метка обязательного поля (`*`) |
| `warning` | предупреждения, «нужно внимание» |
| `secondary` | вторичные действия («Закрыть», «Отмена») |
| `light` | нейтральные плашки: счётчики, пустые состояния |
| `muted` | подсказки, второстепенный текст |

**Правило тёмной темы (важно):** **не использовать текст и иконки цвета `info`**
(`text-info`, иконки с `className="text-info"`) — на тёмном фоне контраст низкий,
читается плохо. Вместо этого:

- акцентный текст/иконки — `primary`, `success` либо нейтральный «корпусный»
  цвет (`text-body` / `text-body-emphasis`), которые сами адаптируются к теме;
- если нужен бейдж `bg="info"` — обязательно задавайте читаемый текст
  (`<Badge bg="info" text="dark">`, как в `components/Devices/Mikrotik/List.jsx`),
  не полагайтесь на цвет «по умолчанию».

В коде ещё встречаются `text-info` и `bg="info"` (например `AiSpeechBadge`,
`AiCategoryBadge`, виджеты дашборда, активные пункты фильтров) — это легаси; в
новых экранах не повторяем, а при касании по возможности заменяем.

---

## Иконки

- Только **react-icons / Remix (`Ri*`)**, размер задаём `size={...}`.
- Закрепившиеся соответствия: `RiSaveLine` — сохранить, `RiArrowGoBackFill` —
  закрыть/назад, `RiAddFill` — добавить, `RiDeleteBinLine` — удалить,
  `RiRefreshLine` — обновить/перегенерировать, `RiCheckLine` — выполнено.
- Иконка обычно идёт перед текстом в кнопке (`<RiSaveLine /> Сохранить`).
- На узких экранах подпись прячут (`d-none d-sm-inline`), оставляя иконку.
- Помним правило тёмной темы: не красить иконки в `info`.

---

## Обратная связь и статусы

- **Тосты** — `UI/AlertToast.jsx` через `store/toast-store.js`
  (`showToast`), контейнер в `Root.jsx` снизу справа. Для кратких уведомлений об
  успехе/ошибке после действия.
- **Инлайн-алерты** — `UI/AlertMessage*`, `Alert` от bootstrap. Внутри форм
  ошибку/успех показывает сам `FormWrapper`.
- **Бейджи статусов** — `Badge` с семантическим `bg`. Карты «статус → вариант»
  собираем рядом с компонентом (примеры в `components/Ticket/Item.jsx`,
  виджетах дашборда).
- **Длительные операции** — `Spinner`; для опроса фонового результата
  паттерн «спиннер + polling» (см. `AiGuide.jsx`, бейджи AI).
- **Пустые состояния** — `AlertMessage variant="light"` с понятным текстом
  («Список пуст»), не пустой экран.

---

## Чек-лист перед мерджем UI

- [ ] Форма создания/редактирования открывается в **Offcanvas снизу** через
      маршрут `add`/`update` и обёрнута в `FormWrapper`.
- [ ] Модалка используется **только** для подтверждения/короткого действия.
- [ ] Многошаговый сценарий имеет **горизонтальный степпер/прогресс** сверху.
- [ ] Обязательные поля помечены `*` (`text-danger`), подсказки — `text-muted`.
- [ ] Выпадающие списки — через `UI/Select` (+ скрытый input для router-формы).
- [ ] Иконки — из набора `Ri*`; нет `text-info`/иконок цвета `info`
      (проверено в **тёмной** теме).
- [ ] Семантика передана bootstrap-вариантами, без кастомных цветов.
- [ ] Мобильная навигация: панели — флекс/absolute-дети `.mobile-shell` (не
      `position:fixed`), скролл только у `.mobile-shell__scroll`, нет слушателей
      скролла на `window`.
- [ ] Проверено в светлой и тёмной теме, на десктопе и мобайле.
